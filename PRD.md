# 투자사업 공사비 적정성 검토 AI 웹 서비스 PRD (인터뷰 기준 원본)

> 개발·테스트·운영 인수의 최신 기준은 [PRD_v2.md](PRD_v2.md)입니다. 이 문서는 기존 인터뷰 결정사항과 상세 배경을 보존한 원본이며, 비판적 검토 결과는 [PRD_REVIEW.md](PRD_REVIEW.md)에 있습니다.

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 문서명 | 투자사업 공사비 적정성 검토 AI 웹 서비스 PRD |
| 대상 서비스 | 광양5 사무동 투자사업 공사비 적정성 검토 MVP(다중 프로젝트 확장형) |
| 기술 구성 | Next.js, FastAPI, PostgreSQL, 파일 저장소, 비동기 작업 큐 |
| 작성 기준일 | 2026-09-09 |
| 기준자료 | `project_plan.md`, `프로젝트_수행흐름_공사비적정성검토.md`, `전처리결과_20260908/119_최종_전처리_종합보고서.md`, `전처리결과_20260908/118_final_preprocessing_status.csv`, `전처리결과_20260908/107_decision_only_queue.csv` |
| 제품 정의 | 도면·내역서·수량산출서·단가자료를 근거로 공사비 적정성을 검토하고 부서별 승인을 관리하는 설명 가능한 웹 서비스 |
| 제외 정의 | 설비 이상탐지, 설비 정상/비정상 분류, 승인 전 자동 확정, 승인 전 데이터를 학습 정답으로 사용하는 AI |

이 문서는 구현팀이 기능·데이터·API·화면·검증 기준을 동일하게 이해하기 위한 제품 요구사항이다. 현재 전처리 결과에 맞춘 MVP 요구사항과, 승인 데이터가 쌓인 뒤 확장할 AI 요구사항을 구분한다.

---

## 1. 제품 개요

### 1.1 문제 정의

공사비 검토에 필요한 도면, 내역서, 수량산출서, 계약단가와 조달청 가격자료가 서로 다른 파일·표기·버전에 분산되어 있다. 현재 자료는 항목 표준화와 후보 연결까지 진행되어 있으나, 도면의 기준/변경 Rev.와 정확한 위치, 수량산출서 행, 내역서 항목, 단가 적용조건을 사람이 확인하고 승인해야 한다. 단순히 최종 합계만 비교하면 공제·할증·이음·정착·LOSS·작업조건 같은 핵심 근거를 놓칠 수 있다.

### 1.2 제품 목표

1. 광양5 사무동 전체를 초기 운영 대상으로 하고, 건물·개별 공사·공종·특정 내역 단위로 검토 범위를 좁혀 조회한다.
2. 다른 프로젝트와 다른 동 자료도 동일한 프로젝트·건물 계층으로 등록·검토할 수 있게 하되, 프로젝트 간 자료·학습·판정 결과가 자동으로 섞이지 않게 한다.
3. 원본 파일은 일반 사용자가 수정·삭제할 수 없고, 해시·버전·경로·시트·행·페이지·좌표를 포함한 근거를 남긴다. `pfm391`만 명시적 삭제 권한을 갖는다.
4. 사용자가 도면·내역서·수량산출서 원본만 업로드하면 시스템이 비동기로 전처리·표준화한 뒤, 규칙 기반으로 산식·수량·금액·단위·규격·Rev. 오류와 도면 변경 후보를 생성한다. 기존 전처리 결과 가져오기는 관리자용 보조 경로로만 제공한다.
5. 공사부서 → 설계부서 → 구매부서 승인 순서를 강제하고 모든 의견·근거·상태변경을 감사 로그로 보존한다.
6. 승인 전 수량·금액·단가는 `검토 후보`, `경고`, `승인 대기`로만 표시하고 확정값으로 자동 반영하지 않는다.
7. 조달청 단가 조회 실패, 인증 실패, 결과 없음, 재시도를 서로 구분하여 사용자에게 보여준다.
8. 동일 입력 자료를 재처리해도 동일한 규칙 버전에서 동일한 결과가 생성되도록 한다.

### 1.3 비목표

- 설비 이상탐지나 센서 시계열 예측 서비스가 아니다.
- 승인되지 않은 자료로 품질 보증용 AI 학습 모델을 만드는 기능은 MVP에 포함하지 않는다.
- 완전 자동 DWG 좌표 판독, 자동 부재 의미 확정, 자동 수량·금액·단가 적용을 약속하지 않는다.
- 다른 프로젝트·다른 동 자료는 등록·검토할 수 있지만 현재 프로젝트 검토 범위에 자동 포함하지 않는다. 타 프로젝트 자료는 신규내역의 참고자료로 명시적으로 선택한 경우에만 조회한다.

### 1.4 기준자료에서 확인된 현재 상태

| 영역 | 현재 전처리 상태 | 제품 처리 원칙 |
|---|---:|---|
| 전처리 결과·보고서 인덱스 | 104개 | 프로젝트/자료 버전과 연결하여 조회 |
| P1 대표 검토 그룹 | 462개 | 프로젝트 범위·공종·내역으로 필터 |
| 공사·설계 결합 의사결정 | 234건 | 근거 확인 및 부서 승인 대기 |
| 조달청 단가 후보 | 86건 | 인증된 재조회·조건 확인 전까지 승인 대기 |
| 도면 변경 후보 | 683건 | 변경 후보로만 표시, 자동 확정 금지 |
| 도면 근거 확인 대기 | 683건 | 설계부서 확인 대상 |
| 공사부서 수량 승인 대기 | 691건 | 공사부서 검토 대상 |
| 자동 산식 검산 | 8,667건 일치 / 24,110건 경고·불가 | 경고와 근거를 보존하고 사람이 판정 |
| 대표 의사결정 확정 연결 | 0건 | 승인 전 확정값 없음 |
| 의사결정 대기열 | 320행 | `107_decision_only_queue.csv`의 승인/수정/추가 근거 요청 대상으로 가져오기·연결 |

`118_final_preprocessing_status.csv` 기준으로 `drawing_quantity`는 234건 전체가 승인 대기이고, `unit_price_api`는 86건 전체가 인증 API 재조회와 파라미터 확인 대기다. 이 상태는 초기 화면과 API에서 그대로 유지한다.

---

## 2. 사용자와 권한

### 2.1 역할 정의

| 역할 | 핵심 책임 | 읽기 | 변경/검토 | 승인 |
|---|---|---|---|---|
| 공사부서 | 수량·산식·금액·공사비 경고 1차 검토 | 전체 프로젝트 | 수량·금액 의견, 추가자료 요청 | 공사 단계 |
| 설계부서 | 기준/변경 도면, Rev., 위치·적용범위 확인 | 전체 프로젝트·도면 | 변경 후보 근거 확인, 수정 | 설계 단계 |
| 구매부서 | 신규내역 단가와 적용조건 검토 | 전체 프로젝트·단가 | 조달청 조회 요청, 후보 비교 | 구매 단계 |
| 전체 관리자·승인권자 | 공사·설계·구매 전체 승인 및 시스템 관리 | 전체 프로젝트·건물·공사 | 사용자·부서·권한·규칙·기준자료·검토 결과 관리 | 공사·설계·구매 전체 단계 및 운영 설정 |
| 관리자 | 사용자·부서·권한·기준자료·규칙·프로젝트 관리 | 전체 | 기준·권한·설정 변경 | 운영 설정 승인 |
| 조회 사용자 | 승인 완료 결과와 이력 조회 | 승인 완료 범위 | 없음 | 없음 |
| CM/검토자(선택) | 범위 내 종합 검토·보고 | 배정 프로젝트 | 의견·추가자료 요청 | 운영 정책에 따라 선택 |

권한은 역할과 기능(공사·설계·구매·관리)을 검사한다. MVP에서는 공사·설계·구매 계정이 전체 프로젝트·건물·공사·내역을 협업 범위로 조회할 수 있고, 기능별 승인 단계만 제한한다. `pfm391`은 전체 기능을 사용할 수 있다. 사용자는 최소권한 원칙으로 기본 거부하며, 승인 API는 현재 단계의 부서 또는 전체 관리자·승인권자만 호출할 수 있다.

### 2.2 MVP 로그인 계정

MVP 개발·검증 환경은 다음 네 개의 초기 계정으로 시작한다.

| 계정 | 역할 | 승인 범위 |
|---|---|---|
| `pfc391` | 공사부서 | 전체 프로젝트의 공사 단계 |
| `pfe391` | 설계부서 | 전체 프로젝트의 설계 단계 |
| `pfp391` | 구매부서 | 전체 프로젝트의 구매 단계 |
| `pfm391` | 전체 관리자·승인권자 | 전체 프로젝트·공사·설계·구매 단계 및 운영 설정 |

사용자가 제공한 초기 비밀번호는 초기 시드 입력으로만 사용한다. PostgreSQL에는 Argon2 또는 bcrypt 해시만 저장하고 평문 비밀번호·시드 원문을 감사 로그나 응답에 남기지 않는다. 첫 로그인 시 비밀번호 변경을 요구한다. 운영 1차는 이 네 개 로컬 계정만 사용하고 SSO/MFA는 후속 단계로 보류한다. 전체 관리자·승인권자는 모든 승인 단계와 사용자·부서·권한·규칙·기준자료·프로젝트 설정을 관리할 수 있다.

### 2.3 감사 대상 행위

로그인, 파일 업로드·재업로드, 버전 매칭, 전처리 연결, 규칙 실행, 경고 판정, 근거 열람·다운로드, 승인·반려·수정·추가자료 요청, 단가 조회·재시도, 규칙·허용오차 변경, 권한 변경, 결과 내보내기를 감사 로그에 기록한다.

---

## 3. 주요 사용자 시나리오

### 시나리오 A: 자료 업로드와 검증

1. 관리자가 프로젝트와 사무동·공사 범위를 선택한다.
2. 사용자가 PDF, DWG, XLSX, XLSM, CSV를 업로드하고 자료 유형(기준/변경 도면, 내역서, 수량산출서, 단가자료), Rev., 기준일, 공종을 입력한다.
3. 시스템은 확장자·MIME·파일 시그니처·크기·바이러스 검사를 통과시킨다.
4. 원본은 지정 파일 저장소에 불변 상태로 보존하고 DB에는 경로, SHA-256 해시, 크기, 생성자, 업로드시각, 자료 유형만 저장한다.
5. 중복 해시는 기존 버전과 연결하며, 새 파일은 비동기 전처리 작업을 생성한다.
6. 검증 실패 시 원인을 분류해 사용자에게 표시하고 파일을 격리한다.

### 시나리오 B: 원본 자료 전처리와 규칙 실행

1. 사용자는 도면·내역서·수량산출서 원본과 기준/변경, Rev., 기준일만 입력한다. 전처리 결과 파일을 업로드할 필요는 없다.
2. 시스템은 원본 파일을 검증·해시 저장하고, PDF/OCR·DWG·엑셀 파서 작업을 비동기로 실행한다.
3. 파서는 원본값·표준화값·산식·도면번호·시트·행·페이지·좌표·매핑 후보를 내부 전처리 결과로 생성한다.
4. 규칙 엔진은 내부 전처리 결과와 규칙 버전·입력 해시를 기록하며 산식·수량·금액·단위·규격·도면번호·시트·Rev.를 검증한다.
5. 오래 걸리는 작업은 작업 ID를 반환하고, 결과는 `일치`, `조건부 일치`, `불일치`, `추가 확인 필요`로 분류하되 모두 검토 후보 또는 경고로 남긴다.

### 시나리오 C: 기존 전처리 결과 가져오기(관리자·회귀검증용)

1. 관리자가 과거 `119`, `118`, `107` 등 기존 산출물을 선택해 특정 프로젝트·자료 버전에 연결한다.
2. 시스템은 파일 해시·형식·프로젝트 범위를 확인하고 `legacy_import` 출처로 기록한다.
3. 일반 사용자는 이 경로를 사용하지 않으며, 기존 결과는 원본 업로드를 대신하지 않는다.
4. 기존 결과는 규칙 회귀 테스트·초기 대기열 보정·운영 전환 비교에 사용하고, 자동 AI 학습 데이터로 사용하지 않는다.

### 시나리오 D: 도면 변경 검토

1. 설계부서는 기준/변경 도면을 도면번호·시트명·Rev.·동·층·공간으로 비교한다.
2. 시스템은 텍스트·객체·레이어·좌표·페이지 차이를 바탕으로 변경 후보를 만든다.
3. 각 후보에 도면번호, 시트, Rev., 위치, GRID/ZONE, 부재·객체 ID, 좌표, 전후 파일 해시, 신뢰도와 판정 사유를 표시한다.
4. 위치·적용범위·수량산출서 행·내역서 항목이 동시에 확인되지 않으면 `추가 확인 필요`로 유지한다.

### 시나리오 E: 수량·산식·금액 검토

1. 공사부서는 도면 입력값 → 산식 각 항 → 산출서 결과값 → 동/층 집계 → 내역서 수량 → 금액 순으로 드릴다운한다.
2. 시스템은 산식을 재계산하고 수량·금액 상대/절대 차이, 단위·규격·품명 불일치, 중복·누락·비정상값을 경고한다.
3. 단위, 규격, 재질, 강도, 공제·할증·이음·정착·LOSS, `내역제외`·`현장반영` 같은 주석 충돌도 금액 차이와 별도로 표시한다.
4. 공사부서는 승인, 수정 요청, 반려, 추가자료 요청 중 하나를 남긴다.

### 시나리오 F: 신규내역 단가 검토

1. 구매부서는 신규내역 후보의 품명·규격·단위·적용조건과 계약단가 유무를 확인한다.
2. 단가 탐색 순서는 동일 프로젝트 타건물의 승인 참고단가 → 공식 조달청 CSV·표준시장단가 → 자료가 없을 때 조달청 PriceInfoService다.
3. 결과 없음, API 인증 실패, 권한 실패, 타임아웃/제한, 검색 조건 불일치를 구분해 기록한다.
4. 어떤 결과도 승인 전 자동 적용하지 않고 후보·근거·조정식·조회일·기준일을 저장한다.

### 시나리오 G: 부서 승인과 이력 재조회

1. 공사부서 승인 완료 후 설계부서 대기열이 열린다.
2. 설계부서가 변경 근거를 확인한 뒤 구매부서 대기열이 열린다.
3. 앞 단계가 반려·수정·추가자료 요청이면 이후 단계는 잠기고 보완 후 공사 단계로 돌아간다.
4. 승인자는 의견·첨부 근거·승인시각을 저장한다.
5. 사용자는 프로젝트·공사·내역·경고·도면번호·기간별로 검토 이력과 감사 로그를 재조회한다.

### 시나리오 H: 근거 기반 검토 챗봇

1. 사용자는 프로젝트·건물·공사·내역 범위를 먼저 선택하고 자연어로 질문한다.
2. 시스템은 로그인 사용자 권한과 선택 범위를 확인한 뒤 경고·수량·도면 변경·단가·승인 이력·전처리 근거를 검색한다.
3. 수량·금액 재계산이 필요한 질문은 AI가 산술을 수행하지 않고 규칙 엔진의 읽기 전용 검산 도구를 호출한다.
4. AI는 검색된 자료 안에서만 답변하며 각 주장에 원본 파일명·해시·시트·행·페이지·도면번호·Rev.·좌표·신뢰도 링크를 붙인다.
5. 근거가 없거나 서로 충돌하면 답을 추정하지 않고 `근거 부족` 또는 `추가 확인 필요`로 표시한다.
6. 챗봇은 승인·반려·단가 확정·원본 수정 같은 상태 변경을 직접 실행하지 않으며, 필요한 경우 해당 검토 화면으로 이동시키는 버튼만 제공한다.
7. 질문·답변·검색 근거·호출한 규칙 도구·모델 버전·권한 범위는 감사 추적을 위해 저장한다.

---

## 4. 업무 흐름과 상태 전이

### 4.1 자료 처리 상태

```text
UPLOADED
  └─> VALIDATING ──┬─> REJECTED (형식/크기/보안 오류)
                   └─> ACCEPTED → PREPROCESSING → LINKED → RULE_CHECKED
                                             └─> FAILED (재시도 가능)
```

원본 파일은 `REJECTED` 또는 재처리 이후에도 기본적으로 삭제하지 않고 격리·실패 사유·해시를 남긴다. `LINKED`는 전처리 결과와 원본 버전의 연결 상태이지 업무 승인 상태가 아니다.

### 4.2 검토 결과 상태

```text
PENDING_REVIEW → IN_REVIEW →
  APPROVED | REVISION_REQUESTED | REJECTED | ADDITIONAL_EVIDENCE_REQUIRED
```

- `PENDING_REVIEW`: 전처리/규칙 결과가 생성되었으나 담당자 미착수
- `IN_REVIEW`: 담당자가 검토를 시작한 상태
- `APPROVED`: 해당 부서 단계의 근거를 확인했지만 최종값 확정은 다음 단계와 전체 조건을 따른다.
- `REVISION_REQUESTED`: 수정 후 같은 부서 또는 공사부서로 되돌린다.
- `REJECTED`: 판정 근거와 반려 사유를 필수로 저장한다.
- `ADDITIONAL_EVIDENCE_REQUIRED`: 파일·페이지·시트·행·도면 위치 등 부족한 증거를 요청한다.

### 4.3 승인 게이트

```text
공사부서 수량/금액 검토
       ↓ 승인
설계부서 도면 변경근거 확인
       ↓ 승인
구매부서 신규내역 단가 검토
       ↓ 승인
전체 조건 충족 시 최종 확정 가능
```

최종 확정 조건은 변경 전후 도면번호·Rev., 위치·적용범위, 수량산출서 산식 또는 행, 내역서 항목, 필요한 단가 근거, 담당 부서 승인 완료가 모두 충족되는 경우뿐이다. 하나라도 빠지면 `검토 후보` 또는 `승인 대기`로 표시한다.

`pfm391` 전체 관리자·승인권자는 긴급 상황에서 승인 순서를 우회할 수 있다. 우회 시 우회 사유, 대상 단계, 대상 검토 결과, 행위자, 시각을 필수 입력하고 감사 로그에 기록한다. 우회 승인이 있더라도 원본 근거와 각 부서 검토 상태를 자동으로 승인 처리하지 않으며, 최종 확정 조건은 별도로 검사한다.

### 4.4 단가 조회 상태

`NOT_REQUESTED → REQUESTED → RUNNING → SUCCEEDED | NO_RESULT | AUTH_ERROR | FORBIDDEN | TIMEOUT | RATE_LIMITED | FAILED → RETRY_PENDING`

상태별 재시도 횟수·마지막 오류·요청 파라미터·응답 요약·API 기준일을 보존한다. 공식 CSV가 정책상 허용되면 API 실패의 대체 근거로 별도 출처 유형을 부여하며, API 결과로 가장하지 않는다.

---

## 5. 기능 요구사항

### 5.1 프로젝트·범위·검색

- 광양5 사무동 프로젝트를 등록하고 건물, 공사/공종, 특정 내역의 계층을 관리한다.
- 전체·건물·공사·내역·도면번호·시트·Rev.·상태·심각도·담당부서·기간으로 필터링한다.
- 다른 프로젝트·다른 동 자료는 현재 프로젝트 검토 범위에 자동 포함하지 않고 `참고자료`로만 명시적으로 검색 가능하게 한다.
- 프로젝트 ID와 건물 ID를 모든 전처리 결과·경고·후보·승인·감사 조회의 필수 범위 조건으로 사용해 프로젝트 간 데이터 혼입을 방지한다.
- 모든 조회 결과는 프로젝트 권한과 자료 버전을 함께 검사한다.

### 5.2 파일·버전·원본 보존

- 허용 확장자: PDF, DWG, XLSX, XLSM, CSV.
- 파일별 초기 최대 크기: PDF 1GB, DWG 500MB, XLSX/XLSM 200MB, CSV 500MB. 한도는 환경변수로 조정한다.
- 확장자와 실제 파일 시그니처, MIME, 파일 크기, 악성코드 검사 결과를 모두 기록한다.
- 원본 파일은 DB BLOB으로 저장하지 않고 파일/오브젝트 저장소의 불변 경로에 저장한다.
- DB는 원본 파일명, 저장 경로, SHA-256, 크기, 업로드자, 업로드일시, 자료 유형, 기준/변경 구분, Rev., 기준일, 프로젝트 연결만 저장한다.
- 동일 해시의 재업로드는 중복 자료로 연결하고, 서로 다른 파일은 새 버전으로 관리한다.
- 동일 SHA-256 재업로드는 기존 원본과 전처리 실행을 재사용하고 중복 업로드 감사 로그만 남긴다.
- 다른 SHA-256의 Rev. 변경 파일은 새 버전·새 전처리 회차로 보존한다. 같은 프로젝트·문서 유형·도면번호·시트·Rev.인데 내용이 다르면 `동일 Rev. 내용 상충` 경고와 근거를 생성한다.
- 새 버전 등록이나 논리 삭제 후에도 이전 전처리 결과·경고·승인 이력은 삭제하지 않고 과거 회차로 재조회할 수 있어야 한다.
- 파일 삭제는 `pfm391`만 수행할 수 있다. 기본 동작은 논리 삭제이며 삭제자·시각·사유·파일 해시·경로를 감사 로그에 남긴다. 물리 삭제는 별도 확인 단계와 보존정책 검사를 통과한 경우에만 허용하고, 공사·설계·구매 계정은 삭제 API를 호출할 수 없다.

### 5.2.1 탐색 구조 원칙

- 데이터 관계는 `프로젝트 → 건물 → 공사/공종 → 내역 항목` 계층으로 유지한다.
- 화면은 계층을 단계별로만 내려가는 방식 대신, 프로젝트 전체를 한 번에 보는 통합 검토 큐를 기본으로 제공한다.
- 모든 검토 화면에는 프로젝트, 건물(전체/선택), 공사·공종, 자료 버전·Rev., 승인 단계·상태 공통 필터를 제공한다.
- 사용자는 통합 큐에서 도면 변경·수량·금액·단가·승인 대기 항목을 선택하고, 상세 패널에서 원본 근거·연결 후보·승인 이력을 확인한다.
- 계층형 상세 경로도 유지하여 특정 프로젝트·건물·공사·내역으로 직접 진입할 수 있어야 한다.
- 선택 일괄승인은 통합 큐에서 선택한 동일 프로젝트·동일 단계 항목에 대해 수행하며, 항목별 결과를 반환한다.

### 5.3 원본 자료 전처리와 기존 결과 연동

#### 5.3.1 운영 기본 경로: 원본만 업로드

- 사용자는 PDF·DWG·XLSX·XLSM·CSV 원본을 업로드하고 자료 유형·기준/변경·Rev.·기준일을 입력한다.
- 시스템은 파일 검증·해시 저장 후 `preprocessing_run`을 생성하고 파서·표준화·매핑·검산 단계를 비동기로 실행한다.
- 파서 결과는 원본값과 표준화값을 분리해 `preprocessing_records`로 저장하고, 파일 해시·페이지·시트·행·셀·좌표를 근거로 연결한다.
- `preprocessing_records`는 `run_id`별로 보존한다. 사용자는 실행 ID·자료 종류·상태·페이지를 선택해 필요한 레코드만 재로딩하여 재검토할 수 있으며, 원본 파일을 다시 업로드하거나 기존 결과를 덮어쓰지 않는다.
- PDF/OCR·DWG 파싱에 실패한 부분은 작업 실패로 전체를 폐기하지 않고 `추가 확인 필요`와 파서 오류를 남긴다. 규칙 엔진은 처리 가능한 항목부터 실행한다.
- 사용자가 전처리 결과 파일을 별도로 준비하지 않아도 규칙 실행과 검토 대기열 생성까지 진행할 수 있어야 한다.

#### 5.3.2 보조 경로: 기존 전처리 결과 가져오기

- `119_최종_전처리_종합보고서.md`, `118_final_preprocessing_status.csv`, `107_decision_only_queue.csv` 등 기존 산출물은 관리자 전용 가져오기·연결 기능으로 제공한다.
- 가져온 결과는 `source_kind=legacy_import`와 원본 해시·생성 시점·규칙 버전을 저장한다.
- 기존 234건 도면/수량 승인 대기, 86건 단가 재조회 대기, 320행 의사결정 대기열은 초기 회귀·운영 전환 데이터로 보존하되, 신규 원본 업로드의 필수 입력으로 만들지 않는다.
- 기존 결과와 새로 생성된 결과가 충돌하면 새 원본 버전을 자동으로 덮어쓰지 않고 별도 검토 후보로 표시한다.

#### 5.3.3 학습과의 구분

- 전처리·표준화·검산은 실행 시마다 수행하는 데이터 처리이며 AI 학습이 아니다.
- 학습은 향후 승인 완료 자료만 별도 데이터셋으로 선정해 수행한다. 런타임 원본 업로드가 학습 작업을 자동으로 시작하지 않는다.

#### 5.3.4 표준 사전 관리

- 표준 품명·규격·단위 사전의 등록·수정·비활성화·버전 발행은 `pfm391` 전체 관리자만 수행한다.
- 공사·설계·구매부서는 원본 근거와 함께 사전 수정 요청을 제출할 수 있으며 직접 기준값을 변경하지 않는다.
- 프로젝트별 예외값은 기준 사전과 분리해 저장하고 적용 범위·시작일·종료일을 기록한다.
- 사전 변경은 새 전처리 실행부터 적용하며 과거 검토 결과를 소급 변경하지 않는다. 변경 전후 값·요청자·승인자·일시는 감사 로그에 남긴다.

### 5.4 도면 변경 검토

- 기준/변경 도면의 도면번호, 시트명, Rev., 동, 층, 공간, GRID, ZONE을 우선 매칭 키로 사용한다.
- PDF는 페이지·텍스트·좌표를, DWG는 레이어·블록·객체 ID·좌표·XREF 정보를 근거로 남긴다.
- 동일 검토 단위는 PDF와 DWG 결과를 하나의 `drawing_change_candidate`로 연결한다.
- CAD 정합성은 공통점 3개 이상을 우선 사용한다. 잔차 5mm 이하 후보는 자동 통과 후보, 5~20mm는 경고, 20mm 초과는 정합 실패로 설계부서 확인을 요구한다.
- 도면번호·Rev.·위치·적용범위·객체가 자동으로 함께 확인되지 않으면 도면 연결 상태를 `연결 후보` 또는 `확인 필요`로 둔다. 도면 연결만 확인되지 않았다는 이유로 `불가` 또는 높은 경고를 생성하지 않는다.
- 도면 후보가 수량산출서 행 또는 내역서 항목과 연결되지 않은 경우에만 해당 연결 부족을 일반 경고로 표시한다. 원본 간 상충·명시적 규격 불일치·산식 오류가 있는 경우는 별도 규칙에 따라 높음 경고로 승격할 수 있다.
- 변경 후보는 수량산출서 행·산식·내역서·경고와 연결 후보를 생성하되, 연결 여부를 자동 확정하지 않는다.

### 5.5 수량·산식·금액 검토

- 산식 원문을 보존하고 파싱 가능한 각 항을 재계산한다.
- 입력값, 산출값, 동/층/공간 집계, 내역서 수량, 단가, 금액을 단계별로 비교한다.
- `수량 × 단가 ≠ 금액`, 산식 합계 불일치, 중복, 누락, 음수·비정상값, 소수/반올림 차이를 경고한다.
- 품명·공종·규격·단위·재질·강도 불일치를 별도 경고로 기록한다.
- 문·창·피트 공제, 헌치·단차, 이음·정착·LOSS, 운반·높이·작업조건 보정, `내역제외`·`업체공사` 주석을 검토 항목에 포함한다.
- 초기 기준값은 개별 내역 금액 규모별로 적용한다. 1천만원 미만은 수량 3%/7%, 금액 5% 또는 30만원/10% 또는 100만원, 1천만원 이상 1억원 미만은 수량 2%/5%, 금액 3% 또는 100만원/7% 또는 300만원, 1억원 이상은 수량 1%/3%, 금액 2% 또는 300만원/5% 또는 1,000만원을 일반/높음 경고 기준으로 사용한다. 프로젝트별 설정 가능하며 변경 이력을 저장한다.
- 도면 연결 후보가 아직 확인되지 않은 것만으로는 높음 경고나 `불가`를 생성하지 않는다. 수량산출서 또는 내역서 연결이 확인되지 않는 경우에는 일반 경고로 표시한다. 단위·규격 불일치, 산식 오류, 원본 간 상충처럼 직접 오류가 확인된 경우는 별도 규칙으로 높음 경고를 생성한다.
- 기준값이 0인 신규/삭제 항목은 상대차 대신 신규/삭제 경고로 분류한다.
- 산출값을 정정하거나 수량·금액을 확정하는 기능은 승인 게이트 이후에만 허용한다.

### 5.6 단가 검토

- 적용 우선순위: (1) 동일 프로젝트 타건물 승인 참고단가, (2) 공식 조달청 CSV·표준시장단가, (3) 자료가 없을 때 조달청 PriceInfoService.
- 후보마다 품명·규격·단위·공종·적용조건·기준일·출처·조회 파라미터·응답·조정식을 저장한다.
- 유사품목 조정은 비교 항목, 조정계수, 조정 사유, 검토자 승인을 필수화한다.
- 조달청 연계 오류는 인증 실패, 권한 거부, 검색 결과 없음, 요청 제한, 타임아웃, 서버 오류로 구분한다.
- 공식 조달청 CSV 또는 표준시장단가 자료가 있으면 API보다 먼저 검토용 적용 후보로 선택한다. 공식 자료가 없을 때만 조달청 API를 조회한다. 후보의 비고에 `대체자료 적용`, 대체 출처, 기준일을 기록하고 API를 생략한 사실도 근거에 남긴다.
- 관리자만 타건물 기계약단가 참고자료를 `price_reference_catalog`에 가져올 수 있다. 저장 시 품명·표준품명·규격·단위·건물·단가·기준일·출처·제한사항·원본 행을 보존하고, 신규내역 조회 시 동일 품명/단위/규격의 참고 후보를 먼저 보여준다. 이 자료는 사무동 확정값·학습 정답으로 혼합하지 않는다.
- 단가 후보의 적용 범위는 출처 프로젝트를 기준으로 구분한다. 동일 프로젝트의 다른 건물에서 품명·규격·단위가 일치하면 `동일 프로젝트 참고 후보`로 자동 제안할 수 있지만, 구매부서 승인 전 확정·내역 덮어쓰기는 금지한다. 다른 프로젝트의 단가는 `외부 프로젝트 참고`로만 조회·표시하고 자동 후보 적용하지 않는다.
- 모든 단가 후보에는 출처 프로젝트·건물·기준일·계약/자료 시기를 표시한다. 프로젝트 기준일과 참고자료 기준일이 다르면 시기 차이 경고를 남기고 사용자가 직접 적용 여부를 판단한다.
- 승인 전 후보 단가는 UI에서 `후보`, `참고`, `검토 대기` 배지로 표시하며 내역서의 확정 단가를 덮어쓰지 않는다.
- 구매부서가 승인한 단가도 전체 최종 승인 전에는 적용 예정값으로만 저장한다.

### 5.7 경고·근거·추적성

모든 경고와 후보는 다음 필드를 갖는다.

- 원본 파일명, 파일 해시, 저장 경로 또는 파일 자산 ID
- 시트명·행·셀 또는 도면번호·시트·Rev.·페이지·좌표·객체 ID
- 원본 값과 표준화 값
- 적용 규칙 ID·버전, 실행 시각, 실행 입력 해시
- 판정 결과, 심각도, 차이값, 판정 사유, 신뢰도, 추가 확인사항
- 연결된 프로젝트·건물·공사·내역·도면 변경 후보
- 검토자·승인자·의견·첨부 근거·상태 변경시각

신뢰도는 사실 확률이 아니라 후보 연결의 근거 강도를 나타낸다. 자동 확정의 대체값으로 사용하지 않는다.

### 5.8 승인·이력

- 부서별 승인 API는 현재 단계, 사용자 부서, 프로젝트 권한, 필수 의견·근거를 검사한다.
- 승인 방식은 개별 처리와 선택 일괄 처리를 모두 제공한다. 일괄 처리는 사용자가 같은 프로젝트·같은 검토 단계·같은 부서의 대상을 직접 선택한 경우에만 허용한다.
- 일괄 처리에서도 각 대상별 선행 승인, 필수 근거, 상태 충돌, 권한을 개별 검사한다. 일부 항목만 성공할 수 있으며 응답에는 항목별 성공·실패 사유를 반환한다.
- 일괄 승인 요청 자체도 감사 로그에 남기고, 실제 상태 변경은 대상별 append-only 승인 이력으로 저장한다. 일괄 처리가 실패한 항목을 자동 재시도하거나 승인으로 간주하지 않는다.
- 수정 요청은 현재 처리 중인 부서 단계에서 다시 검토한다. 반려는 해당 항목에 영향을 주는 승인 단계 중 가장 앞 단계로 되돌리고, 그 이후 단계는 잠근다.
- 승인·수정·반려·추가자료 요청마다 이전 상태, 새 상태, 행위자, 시각, 의견, 첨부 근거, 클라이언트 요청 ID를 기록한다.
- 이전 이력은 수정하거나 덮어쓰지 않고 append-only로 보존한다.
- 동일 요청 ID 재전송은 중복 승인 대신 기존 결과를 반환한다.

### 5.9 비동기 작업

원본 파일 검증 후 전처리·PDF/OCR, DWG 변환·비교, 엑셀 대용량 파싱, 조달청 재조회, 규칙 전체 실행은 작업 큐로 처리한다. 파일은 스트리밍으로 저장하며 무거운 분석 작업은 기본 동시 2개로 시작하고 CPU·메모리에 따라 2~4개로 조정한다. 작업별 제한시간은 30분, 자동 재시도는 최대 5회로 한다. API는 202와 `job_id`를 반환하고, 작업 상태·진행률·재시도·오류·로그를 조회할 수 있어야 한다. 짧은 목록 조회와 승인 저장은 동기 처리한다.

---

## 6. 규칙 엔진과 AI 역할 경계

### 6.1 규칙 엔진(MVP 필수)

- 산식 파싱·재계산, 수량·금액 대조
- 품명·공종·규격·단위·재질·강도 불일치
- 도면번호·시트·Rev.·위치 기준 비교
- 전후 도면 변경 후보·수량·내역·단가 연결 후보
- 단가 소스 우선순위와 조달청 오류 분류
- 경고 심각도·상태·근거·신뢰도 기록
- 규칙 버전·허용오차·기준일의 재현성 보장

### 6.2 AI 보조(후속 단계 또는 제한적 보조)

- 품명·규격 표준화 후보 제안
- 도면 텍스트·객체 변화 후보와 매핑 후보 우선순위 제안
- 신규내역 조달청 검색어 생성
- 경고 사유를 사람이 읽기 쉬운 설명으로 요약
- 검토자가 질문하면 저장된 근거 범위에서 질의응답

AI가 제안한 값은 후보로만 저장하며, 담당자에게 원본 근거를 같이 보여준다. 승인 전 데이터는 학습 정답·평가 정답으로 사용하지 않는다. 충분한 승인 완료 데이터가 축적된 뒤에만 연결 후보·경고 우선순위 모델을 별도 설계한다.

### 6.3 근거 기반 검토 챗봇(챗봇 우선 AI 기능)

챗봇은 자율 에이전트가 아니라 사용자가 질문하면 승인 가능한 자료 범위에서 검색·설명하는 제한형 업무 도우미다. 별도 모델 학습이나 승인 전 자료의 학습 정답화 없이 검색증강생성(RAG)과 규칙 엔진 도구 호출을 사용한다.

#### 처리 순서

1. `POST /chat/sessions/{session_id}/messages`가 사용자·프로젝트·건물·공사 범위와 질문을 받는다.
2. 권한 검사 후 질문에서 의도(현황, 변경 설명, 수량 검산, 단가 비교, 근거 찾기, 승인 상태)와 필터(도면번호, Rev., 층, 내역 코드, 기간)를 구조화한다.
3. PostgreSQL의 프로젝트·전처리 레코드·경고·도면 변경 후보·수량·단가·승인·근거를 먼저 검색하고, 원본 파일 본문은 필요한 페이지·시트·행·객체 주변의 허용된 추출 조각만 읽는다.
4. 수치 재계산·허용오차·승인 순서 판정은 `get_project_status`, `search_findings`, `get_evidence`, `run_targeted_rule_check`, `get_price_candidates`, `get_approval_history` 같은 읽기 전용 도구로 수행한다. AI 모델이 임의 계산하거나 DB를 직접 수정하지 않는다.
5. 모델은 검색된 컨텍스트와 답변 형식 규칙을 받아 한국어 답변을 생성한다. 답변은 `확인된 사실`, `검토 후보`, `추가 확인 필요`, `현재 승인 상태`를 구분한다.
6. 각 답변 문장 또는 항목에는 `evidence_link_id`를 연결하고, 사용자는 원본 파일 열기·검토 화면 이동·관련 경고 보기로 근거를 확인한다.
7. 근거 부족·권한 부족·서로 다른 버전 충돌·도구 실패는 답변에 명시하고, 긴 검색·재검산은 비동기 `job_id`로 전환한다.

#### 챗봇 제한·안전 규칙

- 기본 검색 범위는 현재 사용자의 프로젝트·건물·공사 권한 안으로 제한한다.
- 다른 프로젝트 자료, 승인 전 민감 단가, 개인정보, 원본 전체 파일을 외부 모델에 전송하지 않는다. 외부 모델을 사용할 경우 필요한 추출 조각만 비식별화해 전송한다.
- `승인해`, `단가 확정해`, `수량을 바꿔` 같은 요청은 실행하지 않고 해당 승인·수정 화면으로 안내한다.
- 답변 근거가 하나도 없으면 “자료에서 확인되지 않음”으로 답하고 추정값·가상의 파일 위치를 만들지 않는다.
- 모델 공급자·모델 버전·프롬프트 버전·검색 문서 ID·응답 시간·오류를 `chat_tool_runs`와 감사 로그에 기록한다.
- 운영 초기에는 낮은 창의성 설정과 최대 컨텍스트 수를 고정하고, 모델 장애 시 검색 결과 목록과 규칙 엔진 결과를 직접 보여주는 대체 응답을 제공한다.

---

## 7. 경고 분류와 판정 기준

| 심각도 | 예시 | 시스템 동작 |
|---|---|---|
| 높음 | 수량·금액에 직접 영향이 확인됨, 단위·규격이 명확히 상이함, 산식·원본 간 상충이 확인됨 | 승인 대기 상단, 관련 부서 확인 필수 |
| 중간 | 복수 매핑 후보, 일부 위치·Rev. 미확인, 산식 문자 일부 해석 불가, 단위·규격 조건 일부 미확인 | 검토자 선택과 근거 보완 요구 |
| 일반 | 수량산출서 또는 내역서 연결이 확인되지 않음 | 연결 후보와 원본 근거를 확인하도록 표시 |
| 낮음 | 표기 차이, 보조자료 부족, 반올림 범위 차이, 추가 확인 권장 | 일괄 확인 가능하나 기록 필수 |

판정 값은 `일치`, `조건부 일치`, `불일치`, `추가 확인 필요`, `오탐`으로 구분한다. 도면 연결 미확인은 기본적으로 `연결 후보` 또는 `추가 확인 필요`로 보존하며, 수량산출서·내역서 연결 부족만 일반 경고로 표시한다. 높음 경고도 시스템이 자동으로 불일치를 확정하지 않는다. 검토자가 판정과 사유를 승인해야 한다.

---

## 8. 논리 데이터 모델

DB에는 운영 데이터·검토 결과·승인 이력·추적 메타데이터만 저장하고 대용량 원본은 파일 저장소에 둔다.

| 엔터티 | 주요 필드 | 관계/제약 |
|---|---|---|
| `users` | id, name, email, department_id, status | 사용자·부서 연결 |
| `departments` | id, code, name | 공사/설계/구매/관리 |
| `roles`, `user_roles` | role, scope | 프로젝트·공사 범위까지 권한 적용 |
| `projects` | id, code, name, status, rule_profile_id | 광양5 사무동 프로젝트 |
| `buildings` | id, project_id, name, scope | 프로젝트 하위 |
| `work_packages` | id, building_id, code, name, trade | 공사·공종 |
| `source_files` | id, project_id, type, original_name, path, sha256, size, mime, status | 원본 BLOB 금지, 해시 유일성 |
| `document_versions` | id, source_file_id, kind, before_after, drawing_no, sheet, rev, as_of | 기준/변경 버전 |
| `processing_jobs` | id, type, input_hash, status, progress, retry_count, error_code | 비동기 작업 |
| `preprocessing_runs` | id, project_id, source_file_ids, source_kind, parser_version, input_hash, status, attempt_count, max_attempts, error_message | 원본 자동 전처리 또는 관리자 legacy 가져오기 실행 |
| `preprocessing_records` | id, run_id, source_file_id, raw_value, normalized_value, confidence, source_locator | 내부 전처리 결과 |
| `preprocessing_artifacts` | id, run_id, artifact_type, path, sha256, created_at | 파싱·표준화 산출물 파일 경로와 해시 |
| `price_reference_catalog` | id, project_id, source_file_id, source_scope, original_item, standard_item, specification, unit, building_label, price, reference_date, provenance, restriction, is_active | 동일 프로젝트 타건물 후보·다른 프로젝트 참고 단가; 출처 범위별 자동 적용 제한 |
| `estimate_items` | id, work_package_id, item_code, name, specification, unit, quantity, unit_price, amount, status | 승인 전 값은 후보 상태 |
| `quantity_items` | id, formula_text, parsed_terms, calculated_qty, reported_qty, unit, locator | 산식·수량 |
| `quantity_aggregates` | id, building, floor, space, quantity | 동/층/공간 집계 |
| `drawing_objects` | id, document_version_id, layer, object_id, object_type, grid, zone, coordinates | PDF/DWG 공통 검토 단위 |
| `drawing_change_candidates` | id, baseline_version_id, change_version_id, location, diff, confidence, status | 전후 변경 후보 |
| `mapping_candidates` | id, candidate_type, source_id, target_id, score, reason, status | 도면·수량·내역·단가 연결 |
| `review_runs` | id, project_id, rule_version, input_hash, started_at, completed_at | 재현성 기준 |
| `findings` | id, review_run_id, category, severity, judgement, reason, confidence, status | 경고/판정 |
| `evidence_links` | id, finding_id, source_file_id, page, sheet, row, cell, drawing_no, rev, coordinates, excerpt | 원본 추적 |
| `price_sources` | id, source_type, provider, reference_date, request_params, response_meta | 계약/유사/조달청/표준시장 |
| `price_candidates` | id, estimate_item_id, price_source_id, price, unit, adjustment, status | 승인 전 후보 |
| `price_reviews` | id, price_candidate_id, reviewer_id, judgement, reason | 구매부서 검토 |
| `approvals` | id, target_type, target_id, department, actor_id, from_status, to_status, comment, evidence_ids, created_at | append-only 상태 이력 |
| `audit_logs` | id, actor, action, target, before, after, request_id, ip, created_at | 모든 상태·권한 변경 |
| `reference_dictionaries` | type, source, version, entries | 표준 품명·규격·단위 |
| `review_exports` | id, project_id, review_run_id, export_type, filter_snapshot, approval_snapshot, rule_version, input_hash, data_as_of, file_path, sha256, status | 동일 검토 시점에서 생성한 PDF·Excel·근거 Manifest 패키지 |
| `chat_sessions` | id, user_id, project_id, building_id, work_package_id, scope_snapshot, status, created_at, closed_at | 권한 범위가 고정된 대화 세션 |
| `chat_messages` | id, session_id, role, content, intent, model_provider, model_name, prompt_version, request_id, created_at | 사용자 질문·챗봇 답변 append-only 저장 |
| `chat_citations` | id, message_id, evidence_link_id, source_file_id, locator, quote, confidence | 답변 항목별 원본 근거 연결 |
| `chat_tool_runs` | id, message_id, tool_name, input_snapshot, output_snapshot, status, latency_ms, error_code | 검색·규칙 도구 호출과 실패 추적 |

금액·단가·수량 필드는 원본 값과 표준화 값을 분리하고, 확정 여부를 나타내는 `approval_state`를 둔다. `APPROVED`가 아니면 API와 UI가 확정값으로 직렬화하지 않는다.

---

## 9. API 요구사항

모든 API는 인증 토큰, 프로젝트 권한, 입력 스키마, 페이지네이션, 오류 코드, 요청 ID를 사용한다. 표준 오류 응답은 `code`, `message`, `details`, `request_id`, `retryable`을 포함한다.

### 9.1 상태·프로젝트·자료

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET | `/health` | 서비스·DB 상태 확인 |
| GET | `/api/v1/projects` | 권한 범위의 프로젝트 목록·현황 |
| GET | `/api/v1/projects/{project_id}` | 프로젝트·건물·공사 요약 |
| POST | `/api/v1/projects/{project_id}/files` | 파일 업로드, 검증, 메타데이터 저장 |
| GET | `/api/v1/projects/{project_id}/files` | 자료·버전·해시·검증 상태 조회 |
| DELETE | `/api/v1/projects/{project_id}/files/{file_id}` | 전체 관리자 전용 원본 논리 삭제·감사 기록 |
| GET | `/api/v1/projects/{project_id}/preprocessing` | 전처리 상태·결과·대기 건수 조회 |
| POST | `/api/v1/projects/{project_id}/preprocessing/run` | 원본 파일 기반 전처리·표준화 작업 생성(202) |
| POST | `/api/v1/projects/{project_id}/preprocessing/{run_id}/retry` | 실패한 원본 전처리 재접수(최대 5회 자동 재시도 정책) |
| POST | `/api/v1/admin/projects/{project_id}/preprocessing/import` | 관리자 전용 기존 전처리 결과 가져오기·연결(202) |
| GET | `/api/v1/projects/{project_id}/preprocessing/runs/{run_id}/records` | 선택 실행의 원본값·표준화값을 종류·상태·offset/limit로 재조회 |
| GET | `/api/v1/jobs/{job_id}` | 비동기 작업 상태·진행률·오류 조회 |

### 9.2 규칙·검토·근거

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/v1/projects/{project_id}/review-runs` | 규칙 실행 작업 생성(202) |
| GET | `/api/v1/projects/{project_id}/findings` | 경고·판정·심각도·상태 필터 조회 |
| GET | `/api/v1/projects/{project_id}/drawings/changes` | 도면 변경 후보 조회 |
| GET | `/api/v1/projects/{project_id}/quantities` | 산식·수량·금액 검산 결과 조회 |
| GET | `/api/v1/projects/{project_id}/mappings` | 도면·수량·내역 연결 후보 조회 |
| GET | `/api/v1/findings/{finding_id}/evidence` | 경고 근거와 원본 위치 조회 |
| GET | `/api/v1/projects/{project_id}/review-history` | 검토·승인·재처리 이력 조회 |

### 9.3 승인·단가

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/v1/reviews/{review_id}/approvals` | 승인/수정/반려/추가자료 요청; 단계·권한 검사. `pfm391` 순서 우회 시 사유 필수 |
| POST | `/api/v1/projects/{project_id}/reviews/approvals/batch` | 사용자가 선택한 동일 단계 대상의 일괄 승인; 항목별 성공·실패 결과 반환 |
| GET | `/api/v1/projects/{project_id}/approval-queue` | 부서별 승인 대기열 |
| POST | `/api/v1/projects/{project_id}/prices/lookup` | 조달청/기타 공식 단가 조회 작업 생성(202) |
| GET | `/api/v1/projects/{project_id}/prices/results` | 단가 후보·출처·오류·검토 상태 |
| GET | `/api/v1/projects/{project_id}/price-references` | 저장된 참고 단가를 품명 검색·비교용으로 조회 |
| POST | `/api/v1/admin/projects/{project_id}/price-references/import` | 관리자 전용 참고 단가 CSV 적재 및 출처·제한사항 저장 |
| GET | `/api/v1/projects/{project_id}/prices/{result_id}/decision` | 단가 적용 판단 조회 |
| POST | `/api/v1/projects/{project_id}/prices/{result_id}/decision` | 구매부서 단가 후보·보류 판단 저장, 자동 확정 금지 |
| POST | `/api/v1/price-candidates/{candidate_id}/review` | 구매부서 단가 판단 저장 |

### 9.4 검토 챗봇

| 메서드 | 경로 | 설명 |
|---|---|---|
| POST | `/api/v1/projects/{project_id}/chat/sessions` | 프로젝트·건물·공사 범위가 고정된 챗봇 세션 생성 |
| GET | `/api/v1/projects/{project_id}/chat/sessions` | 사용자가 접근 가능한 자신의 챗봇 세션 목록 |
| POST | `/api/v1/projects/{project_id}/chat/sessions/{session_id}/messages` | 질문 입력, 근거 검색·규칙 도구 호출·답변 생성(일반 200, 장기 작업 202) |
| GET | `/api/v1/projects/{project_id}/chat/sessions/{session_id}/messages` | 질문·답변·상태·인용 근거 조회 |
| GET | `/api/v1/projects/{project_id}/chat/messages/{message_id}/citations` | 답변에 사용한 파일·시트·행·페이지·도면번호·Rev.·좌표 조회 |

챗봇 API는 `project_id`, 세션의 `scope_snapshot`, 사용자 권한, 입력 길이·첨부 파일 여부를 검증한다. 응답에는 `answer`, `answer_state`, `confidence`, `citations`, `next_actions`, `request_id`를 포함한다. `answer_state`는 `근거 확인`, `검토 후보`, `추가 확인 필요`, `권한 부족`, `처리 실패` 중 하나이며 `확정`을 의미하지 않는다.

운영 설정은 `CHAT_MODEL_PROVIDER`, `CHAT_MODEL_NAME`, `CHAT_API_KEY`, `CHAT_TIMEOUT_SECONDS`, `CHAT_MAX_CONTEXT_ITEMS`, `CHAT_ALLOW_EXTERNAL_MODEL`로 분리한다. API 키는 환경변수·비밀 저장소로만 주입하고 DB나 대화 내용에 저장하지 않는다.

### 9.5 관리자·운영(관리자 전용 또는 후속)

| 메서드 | 경로 | 설명 |
|---|---|---|
| GET/PUT | `/api/v1/admin/rules` | 규칙·허용오차·버전 관리 |
| GET/PUT | `/api/v1/admin/users`, `/departments` | 사용자·부서·권한 관리 |
| GET | `/api/v1/projects/{project_id}/audit-logs` | 프로젝트별 삭제·상태 변경 감사 로그 조회 |
| POST | `/api/v1/projects/{project_id}/reports` | 검토표·요약·근거 추적 보고서 생성 |
| GET | `/api/v1/projects/{project_id}/reports/{export_id}` | 검토 결과 패키지 생성 상태·파일 경로·기준 스냅샷 조회 |
| GET | `/api/v1/projects/{project_id}/reports/{export_id}/download` | 준비된 bundle·Excel·PDF·Manifest 다운로드 |

승인 API는 `current_status`, `department`, `required_evidence`, `idempotency_key`를 검사한다. 권한 부족은 403, 자료 없음은 404, 중복/상태 충돌은 409, 입력 오류는 422, 외부 단가 연계 오류는 502/504 계열과 내부 오류 코드로 분리한다.

---

## 10. 화면 요구사항

| 화면 | 핵심 내용 | 필수 상호작용 |
|---|---|---|
| 프로젝트 현황 대시보드 | 프로젝트·건물·공사별 진행률, 경고, 승인 대기, 단가 오류 | 공통 범위 필터, 통합 큐 이동, 상세 패널 열기 |
| 자료 업로드·버전 관리 | 원본 업로드, 기준/변경 구분, 해시·검증·전처리 작업 상태 | 업로드 후 전처리 시작, 재업로드, 버전 비교, 실패 재시도 |
| 전처리 상태 | 원본별 파싱·표준화·검산 작업과 234/86 기존 대기 자료 | 작업 상세·오류·재실행, 관리자용 기존 결과 가져오기 |
| 도면 변경부위 검토 | 통합 큐의 전후 도면번호·시트·Rev., 동/층/공간/GRID/ZONE, 후보 오버레이 | 공통 필터, 후보 채택·수정·추가근거 요청, 상세 패널 |
| 수량·산식·금액 검토 | 통합 큐의 산식 원문, 재계산값, 집계, 내역, 차이·경고 | 공통 필터, 행 단위 판정·의견·승인, 상세 패널 |
| 신규내역 단가 검토 | 통합 큐의 4단계 가격 소스, 검색 조건, 응답·조정식·오류 | 공통 필터, 재조회·후보 선택·적용 판단, 상세 패널 |
| 부서별 승인 대기열 | 공사→설계→구매 단계별 통합 대기 | 공통 필터, 개별 선택, 일괄 선택 후 승인·반려·수정·자료 요청 |
| 경고 상세·원본 근거 패널 | 파일명·해시·시트/행/페이지/좌표·원본/표준값·신뢰도 | 원본 열기·근거 다운로드 |
| 검토 이력·감사 로그 | 상태 전이, 사용자·부서·의견·시각, 재처리 비교 | 기간·대상·행위 필터 |
| 보고서 | 동일 스냅샷의 요약 PDF·상세 Excel·근거 Manifest·원본 링크 목록 | 승인 전 값 표시 규칙 적용, export_id·검토 회차·규칙 버전 고정 |
| 검토 챗봇 | 프로젝트·건물·공사 범위가 고정된 한국어 질문·답변, 답변별 근거·신뢰도·상태 | 질문 입력, 추천 질문, 원본 근거 열기, 검토 화면 이동, 장기 작업 상태 확인 |

UI에서 수량·금액·단가는 승인 전 항상 `후보값`, `검토 대기`, `자동 확정 안 됨`을 표시한다. 확정 스타일·확정 합계·다운로드 보고서에는 전체 승인 게이트를 통과한 값만 확정 표현을 사용할 수 있다.

### 10.1 검토 결과 패키지

- 웹 화면과 다운로드 파일이 서로 다른 시점의 데이터를 사용하지 않도록 하나의 `export_id` 스냅샷에서 PDF·Excel·근거 Manifest를 함께 생성한다.
- PDF는 프로젝트·건물·공사 현황, 주요 경고, 승인 진행률, 미결정·추가자료 요청을 요약한다. 상세 행 전체보다 결재·공유에 필요한 요약을 우선한다.
- Excel은 `요약`, `도면변경`, `수량산식`, `내역연결`, `단가검토`, `승인이력`, `원본근거`, `데이터정의` 시트를 포함한다.
- 근거 Manifest(CSV 또는 JSON)는 원본 파일명·해시·경로, 시트·행·셀·페이지·좌표, 경고 ID, 규칙 버전, 입력 해시, 신뢰도, 승인 상태를 제공한다.
- 원본 PDF·DWG·엑셀 본문은 보고서에 복사하지 않고 파일 경로와 근거 위치만 연결한다.
- 승인 전 패키지는 표지·파일명·각 수량/금액/단가에 `검토 후보`, `승인 대기`, `확정 전`을 표시한다. 전체 승인 완료 후에만 확정 결과 패키지를 생성한다.
- 대용량 보고서는 비동기 작업으로 생성하며 생성 기준일, 필터, 자료 버전, 승인 상태, 규칙 버전, 입력 해시를 `review_exports`에 저장한다.

---

## 11. 오류 처리와 사용자 메시지

| 상황 | API 동작 | 사용자 표시/조치 |
|---|---|---|
| 확장자·시그니처 불일치 | 422, 파일 격리 | “파일 형식이 실제 내용과 다릅니다. 원본 형식을 확인하세요.” |
| 파일 크기 초과 | 413 | 허용 크기와 대용량 비동기 처리 방법 표시 |
| 악성코드 의심 | 422/403, 접근 차단 | “보안 검사에 실패해 격리되었습니다. 관리자에게 문의하세요.” |
| 중복 해시 | 409 또는 기존 버전 반환 | 기존 파일·버전 연결 여부 선택 |
| 전처리 결과 해시 불일치 | 409 | 원본 버전 재선택 요청, 임의 연결 금지 |
| PDF/DWG/엑셀 분석 실패 | 작업 `FAILED`, retryable 여부 | 단계·파일·페이지/시트·재시도 버튼 표시 |
| DB 연결/트랜잭션 오류 | 503/500, 요청 ID 저장 | “저장에 실패했습니다. 변경하지 않았습니다.” |
| 권한 부족 | 401/403 | 숨김 대신 접근 불가와 필요한 역할 표시 |
| 승인 상태 충돌 | 409 | 최신 상태 새로고침 후 재검토 안내 |
| 조달청 인증/권한 오류 | `AUTH_ERROR`/`FORBIDDEN` | “조달청 인증/권한을 확인하세요. 단가를 적용하지 않았습니다.” |
| 조달청 검색 결과 없음 | `NO_RESULT` | 공식 CSV·표준시장단가가 있으면 대체자료 후보를 자동 생성하고 비고에 대체 출처를 기록 |
| 조달청 제한/타임아웃 | `RATE_LIMITED`/`TIMEOUT` | 자동 재시도 시각, 수동 재시도, 공식 CSV 정책 안내 |
| 외부 서버 오류 | `FAILED` | 공식 대체자료가 있으면 검토용 후보로 선택하고, 재시도 가능 여부와 마지막 응답 시각 표시 |
| 근거 누락 | 결과 `ADDITIONAL_EVIDENCE_REQUIRED` | 파일·시트·행·페이지·도면번호 등 필요한 항목 체크리스트 |
| 챗봇 근거 없음 | 200, `answer_state=추가 확인 필요` | “현재 자료에서 확인되지 않습니다. 원본 근거를 추가하거나 범위를 좁혀 주세요.” |
| 챗봇 권한 부족 | 403 또는 검색 결과 비노출 | “현재 계정의 프로젝트·부서 범위에서 조회할 수 없습니다.” |
| 챗봇 모델·검색 시간 초과 | 202 작업 전환 또는 `503`, `retryable=true` | “답변 생성이 지연되고 있습니다. 작업 상태를 확인하거나 규칙 결과를 직접 보세요.” |
| 챗봇 출처 버전 충돌 | 200, `answer_state=추가 확인 필요` | “기준/변경 Rev.가 충돌합니다. 승인 전 판단하지 말고 버전을 확인하세요.” |

모든 사용자 메시지는 오류 코드와 요청 ID를 포함하고, 기술 상세는 로그에 남기되 화면에는 조치 가능한 문장으로 보여준다.

---

## 12. 보안·권한·운영

- 모든 쓰기 API는 인증과 역할·프로젝트 범위 권한을 검사한다.
- MVP 초기 계정 4개(`pfc391`, `pfe391`, `pfp391`, `pfm391`)를 운영 1차 계정으로 사용하고, 운영 배포 시 `AUTH_REQUIRED=true`와 첫 로그인 비밀번호 변경을 강제한다.
- SSO/MFA는 후속 운영 단계로 보류하며, 계정 추가·권한 변경은 `pfm391` 승인과 감사 로그를 거친다.
- 전송 중 TLS, 저장 중 파일·DB 암호화, 조달청 키·DB 비밀번호의 비밀 저장소 관리를 사용한다.
- 원본 파일은 DB에 넣지 않고 접근 제어된 경로/오브젝트 저장소에 저장한다. 다운로드도 권한과 감사 로그를 검사한다.
- 업로드 파일은 확장자만 신뢰하지 않고 시그니처·MIME·악성코드 검사를 통과해야 한다.
- 프로젝트·건물·공사 스코프를 모든 쿼리의 조건으로 사용해 다른 프로젝트 정보가 노출되지 않게 한다.
- 원본·전처리·검토 결과는 프로젝트 종료 후 기본 7년, 승인 이력·감사 로그는 최소 7년 보존한다. 진행 중인 프로젝트는 보존기간이 지나도 자동 삭제하지 않는다.
- `pfm391`의 삭제 행위도 감사 로그·해시·메타데이터로 보존하며, 물리 삭제는 보존기간·법적 보류·백업 상태 확인 후 별도 승인한다.
- PostgreSQL과 원본 저장소는 매일 증분·주 1회 전체 백업을 수행하고, 최근 35일 복구본과 월별 장기 백업을 유지한다. 월별 백업은 기본 7년 보관하며 실제 복구 테스트를 정기 수행한다.
- AI 외부 전송 시 원본 파일·개인정보·미승인 가격자료를 전송하지 않으며, 허용된 근거만 비식별화해 사용한다.
- 챗봇 세션은 사용자·프로젝트·건물·공사 범위를 세션 생성 시 고정하고, 매 메시지마다 권한을 다시 확인한다. 검색 결과에 포함되지 않은 프로젝트 자료는 모델 컨텍스트에도 넣지 않는다.
- 챗봇 답변·인용·도구 실행은 감사 로그에 남기며, 챗봇에는 승인·반려·단가 확정·원본 수정 쓰기 도구를 제공하지 않는다.
- 외부 모델 사용 여부와 모델명·프롬프트 버전·보존기간은 환경변수와 운영 설정으로 관리하고, 원본 파일 전체를 외부 모델에 전송하지 않는다.
- 감사 로그는 애플리케이션 사용자가 수정할 수 없고, 관리자 조회도 별도 행위로 기록한다.

---

## 13. 비기능 요구사항

### 성능·확장성

- 목록 API는 페이지네이션과 필터를 지원하고 기본 2초 이내 응답을 목표로 한다(외부 연계 제외).
- 대용량 PDF·DWG·엑셀은 웹 요청을 점유하지 않고 작업 큐와 파일 스트리밍으로 처리한다. 파일 유형별 크기 한도와 워커 동시성은 환경변수로 조정한다.
- 동일 작업 중복 요청은 입력 해시·멱등키로 합치거나 기존 작업을 반환한다.
- 대시보드는 수천 건 경고를 가상 스크롤 또는 서버 페이지네이션으로 처리한다.
- 챗봇은 일반 근거 검색·요약 질문의 첫 답변을 목표 10초 이내에 반환하고, 대량 검색·재검산은 202 작업과 진행 상태로 전환한다. 모델 장애 시 검색된 근거 목록과 규칙 엔진 결과를 대체 응답으로 제공한다.

### 재현성·정확성

- 결과마다 규칙 버전, 기준자료 버전, 입력 파일 해시, 실행 시각, 파서 버전을 저장한다.
- 허용오차·단가 정책 변경은 새 버전으로만 적용하고 과거 결과를 변경하지 않는다.
- 동일 자료 재처리 시 동일한 결정적 규칙 결과를 생성한다.
- 정확한 변경 부위 자동 후보는 목표 IoU 0.90 이상·누락 5% 이하를 지향하고, 목표 미달 시 수동 확인으로 전환한다.

### 사용성·관측성

- 한국어 UI, 키보드 접근성, 색상만으로 심각도를 구분하지 않는 배지·아이콘을 사용한다.
- 구조화 로그, 작업 지연·실패율, 조달청 오류율, 승인 SLA를 모니터링한다.
- 모든 화면에서 “왜 경고인지”, “어떤 파일의 어느 위치인지”, “누가 언제 판정했는지”를 한 번에 확인할 수 있어야 한다.

---

## 14. MVP 범위와 제외 범위

### 14.1 MVP에 포함

1. 광양5 사무동 프로젝트 등록 및 다른 프로젝트·건물 추가가 가능한 계층 구조
2. PDF/DWG/XLSX/XLSM/CSV 자료 업로드와 버전·해시·경로 관리
3. 원본 자료 업로드 후 비동기 전처리·표준화·검산 결과 생성
4. 도면 변경 후보 조회
5. 수량·산식·금액 검산 결과 조회
6. 경고와 원본 근거 표시
7. 공사→설계→구매 승인 흐름 및 감사 이력
8. 조달청 단가 조회 요청·결과·오류 저장
9. 프로젝트 현황·도면·수량·단가·승인·이력 화면
10. 프로젝트·건물·공사 범위가 고정된 근거 기반 검토 챗봇과 답변 인용
11. FastAPI 입력 검증·오류 응답·권한 검사
12. PostgreSQL 마이그레이션·샘플 데이터·환경변수 예시·통합 테스트

### 14.2 MVP에서 제외하거나 수동 처리

- AI 모델 학습 및 승인 전 데이터 자동 학습
- 모든 DWG의 완전 자동 의미 해석과 좌표 확정
- 타 건물 통합 학습·평가·자동 기준값 전이
- 승인 전 수량·금액·단가의 자동 확정·내역서 덮어쓰기
- 외부 단가자료를 조달청 결과와 구분 없이 자동 혼합
- 승인·반려·단가 확정·원본 수정까지 자율 실행하는 에이전트
- 계약·발주·ERP의 자동 집행 연계
- 결재 문서의 법적 전자서명 연동(운영 정책 확정 후 후속)

---

## 15. 향후 AI 확장 계획

### 단계 1: 규칙 기반 운영(MVP)

입력 검증, 표준화 결과 연결, 산식·수량·금액·Rev. 규칙, 후보·경고·근거·승인 이력과 읽기 전용 근거 기반 검토 챗봇을 안정화한다. 챗봇은 규칙 결과와 원본 근거를 설명하지만 승인·확정은 수행하지 않는다.

### 단계 2: 근거 기반 검토 챗봇

프로젝트·건물·공사 범위가 고정된 챗봇을 제공한다. 품명·규격 표준화, 도면 변화 후보, 매핑 후보 순위, 조달청 검색어, 경고 설명, 검토 이력 질문에 답하되, 모든 답변을 원본 근거 링크와 함께 저장한다. 챗봇은 읽기 전용 도구만 호출하고 승인·반려·단가 확정은 수행하지 않는다.

### 단계 3: 승인 데이터 기반 모델

충분한 승인 완료·반려·수정 데이터가 쌓이면 데이터 품질 검증, 프로젝트 분리, 라벨 정의, 학습/평가 분리를 먼저 만든다. 연결 후보 우선순위와 경고 우선순위 모델부터 검증하고, 자동 확정은 별도 승인 정책 없이는 제공하지 않는다.

### 단계 4: 제한형 에이전트와 보고서 자동화

승인된 프로젝트 자료만 검색하는 고도화 RAG, 근거 추적 보고서 초안, 사용자가 명시적으로 승인한 재검산·재조회 작업을 제공한다. 쓰기 작업은 항상 별도 승인 버튼과 부서 권한을 거치며, 검색 결과에는 원본 위치·버전·승인 상태를 포함한다.

---

## 16. 테스트 및 성공 기준

### 16.1 필수 통합 흐름

```text
원본 자료 업로드
 → 파일 검증·해시 저장
 → 비동기 전처리·표준화·근거 생성
 → 규칙 실행
 → 도면 변경 후보·수량/산식/금액 경고 생성
 → 공사부서 승인
 → 설계부서 근거 확인
 → 구매부서 단가 검토
 → PostgreSQL 승인/감사 이력 저장
 → Next.js 결과 표시
 → 과거 이력 재조회
```

기존 전처리 결과 가져오기는 관리자용 보조 경로이며, 일반 사용자의 정상 운영 흐름에는 포함되지 않는다.

### 16.2 인수 기준

| 영역 | 합격 기준 |
|---|---|
| 원본 보존 | 일반 계정이 원본을 수정·삭제할 수 없고, 업로드 전후 해시가 보존된다. `pfm391` 삭제 시에도 삭제 이력·해시·추적 메타데이터가 남으며 DB에는 경로·추적정보만 있다. |
| 전처리 연동 | 원본만 업로드해도 전처리 작업·규칙 실행·검토 대기열이 생성된다. 기존 234건 도면/수량 대기와 86건 단가 대기는 별도 가져오기 데이터로 승인 완료로 오인되지 않는다. |
| 규칙 결과 | 산식·수량·금액·단위·규격·Rev. 경고가 규칙 버전·입력 해시와 함께 생성된다. |
| 추적성 | 모든 경고가 파일명/해시, 시트·행·셀 또는 도면번호·페이지·좌표 중 유효한 원본 위치를 가진다. |
| 승인 순서 | 공사 승인 전 설계·구매 승인을 호출할 수 없고, 설계 승인 전 구매 대기열이 열리지 않는다. |
| 확정 방지 | 최종 승인 전 API와 UI가 수량·금액·단가를 확정값으로 저장·표시·내보내지 않는다. |
| 단가 오류 | 인증 실패·권한 거부·검색 결과 없음·제한·타임아웃·서버 오류가 서로 구분된다. |
| 권한 | 부서·프로젝트 범위 밖 자료 조회·변경·승인이 401/403으로 차단된다. |
| 대용량 | 대용량 PDF·DWG·엑셀 요청이 202 작업으로 전환되고 진행률·실패·재시도가 조회된다. |
| 재현성 | 동일 자료·동일 규칙 버전 재처리 결과의 경고 ID 유형·판정·근거가 일관된다. |
| 이력 | 승인·반려·수정·추가자료 요청과 재조회가 PostgreSQL에서 순서대로 확인된다. |
| 화면 | Next.js 각 화면에서 경고 사유·원본 파일·도면번호·시트·행·신뢰도가 표시된다. |
| 챗봇 | 권한 범위 밖 자료가 검색되지 않고, 모든 답변이 하나 이상의 근거 또는 `근거 없음` 상태를 가지며, 승인·단가 확정 요청을 실행하지 않는다. |

### 16.3 테스트 구성

- 단위 테스트: 산식 파서, 단위 변환, 상대/절대 차이, 심각도, 승인 상태 전이, 단가 오류 분류
- API 테스트: 2xx/4xx/409/5xx, 입력 스키마, 권한, 멱등성, 페이지네이션
- 통합 테스트: 전처리 샘플 → 규칙 엔진 → FastAPI → PostgreSQL → Next.js 표시
- 파일 테스트: 정상/위조 확장자/대용량/중복 해시/악성코드 격리 샘플
- 외부 연계 테스트: 조달청 성공, 인증 오류, 권한 오류, 빈 결과, 429, 타임아웃, 5xx와 재시도
- 승인 테스트: 순서 강제, 반려 루프, 추가자료 요청, 부서별 의견·근거·감사 로그
- 회귀 테스트: 승인 전 값이 확정 상태로 바뀌지 않는지, 과거 결과가 규칙 변경으로 변하지 않는지
- 챗봇 테스트: 프로젝트·건물 권한 격리, 근거 인용 누락 방지, 근거 없음 응답, Rev. 충돌, 모델·검색 타임아웃, 규칙 도구 호출 결과, 승인·단가 확정 요청 거부
- UI 테스트: 대기·오류·권한 없음·근거 누락 메시지와 접근성, 챗봇 인용 링크·검토 화면 이동

---

## 17. 실행·배포 전 인터뷰 및 확정 항목

다음은 개발 착수 전에 담당자와 확정한다. 미확정인 경우 PRD 기본값을 적용하되 관리자 설정으로 변경 가능하게 한다.

1. **확정 — 운영 범위**: 첫 운영 대상은 광양5 사무동 전체이며 건물·공사·공종·특정 내역으로 필터링한다. 다른 프로젝트와 다른 동 자료도 프로젝트·건물 단위로 등록할 수 있으나 프로젝트 간 자동 혼합은 금지한다.
2. **확정 — 금액 규모별 차등 기준**: 개별 내역 금액 1천만원 미만은 수량 3%/7%, 금액 5% 또는 30만원/10% 또는 100만원, 1천만원 이상 1억원 미만은 수량 2%/5%, 금액 3% 또는 100만원/7% 또는 300만원, 1억원 이상은 수량 1%/3%, 금액 2% 또는 300만원/5% 또는 1,000만원을 일반/높음 경고 기준으로 적용한다. 도면 연결 미확인은 불가로 처리하지 않고, 수량산출서 또는 내역서 연결 미확인만 일반 경고로 표시한다.
3. **확정 — 승인 흐름**: 공사부서 검토·승인 → 설계부서 도면 변경근거 확인·승인 → 구매부서 단가 검토·승인 순서로 진행한다. 수정 요청은 현재 부서 단계에서 재검토하고, 반려는 해당 항목에 영향을 주는 승인 단계 중 가장 앞 단계로 되돌리며 이후 단계는 잠근다. 추가자료 요청도 현재 단계의 대기 상태로 유지한다. 각 부서 검토 기한은 영업일 3일로 하며 기한 초과 시 자동 승인하지 않고 `pfm391`에게 지연 알림을 보낸다. 대리 승인은 `pfm391`이 기간·범위·사유를 등록한 경우에만 허용하고 대리 승인자·기간·대상·사유를 감사 로그에 남긴다.
4. **확정 — 단가 자료 우선순위**: 동일 프로젝트 타건물의 승인 참고단가를 먼저 확인하고, 공식 조달청 CSV·표준시장단가가 있으면 API보다 먼저 검토용 적용 후보로 선택한다. 공식 자료가 없을 때만 조달청 API를 조회한다. 비고에 `대체자료 적용`, 출처, 기준일, 적용 사유를 기록하며 구매부서·전체 승인 전에는 확정 단가로 반영하지 않는다. 조달청 API 연동 코드는 구현되어 있고 URL·서비스명·키는 환경변수로 주입한다. 실제 운영 배포 시 비밀키를 Secret으로 등록하고 호출 한도만 운영 담당자가 확인한다.
5. **확정 — 보존·삭제·백업**: 원본·전처리·검토 결과는 프로젝트 종료 후 기본 7년, 승인 이력·감사 로그는 최소 7년 보존한다. `pfm391`만 삭제할 수 있으며 기본은 논리 삭제, 물리 삭제는 보존기간·법적 보류·백업 상태 확인 후 허용한다. PostgreSQL과 원본 저장소는 매일 증분·주 1회 전체 백업, 최근 35일 복구본, 월별 7년 장기 백업을 적용하고 정기 복구 테스트를 수행한다.
6. **확정 — MVP 인증·권한**: 운영 1차는 `pfc391`(공사), `pfe391`(설계), `pfp391`(구매), `pfm391`(전체 관리자·승인권자) 네 개 로컬 계정만 사용한다. 공사·설계·구매 계정은 전 프로젝트를 협업 범위로 조회·검토하고 각 부서 승인 단계만 처리한다. `pfm391`은 모든 승인 단계와 사용자·부서·권한·규칙·기준자료·프로젝트 설정을 관리하며 긴급 상황에 승인 순서를 우회할 수 있다. 우회 사유·대상·행위자·시각은 필수 감사 기록으로 남긴다. 비밀번호는 해시로 저장하고 첫 로그인 변경을 요구하며, 운영 환경은 `AUTH_REQUIRED=true`로 설정한다. SSO/MFA는 후속 단계로 보류한다.
7. **확정 — 대용량·CAD 처리**: 파일별 초기 최대 크기는 PDF 1GB, DWG 500MB, XLSX/XLSM 200MB, CSV 500MB로 한다. PDF/OCR·DWG·대용량 엑셀 분석은 스트리밍·비동기 처리하고, 무거운 작업은 기본 동시 2개, 환경에 따라 2~4개로 조정한다. 작업 제한시간 30분, 자동 재시도 최대 5회를 적용한다. 전처리에서 사용한 ZWCAD·CAD 뷰어·추출도구를 우선 Windows 분석 워커에서 재사용하고, FastAPI/PostgreSQL 컨테이너와 분리한다. 도구의 버전·실행 옵션·라이선스 범위·결과 해시를 기록하며, 현재 라이선스로 부족한 동시 처리량이나 자동화 권한이 확인될 때만 추가 라이선스를 도입한다. CAD 도구가 없는 환경에서는 DWG 메타데이터·원본 경로만 보존하고 자동 확정하지 않는다.
8. **확정 — 표준 사전 관리**: 표준 품명·규격·단위 사전의 등록·수정·비활성화·버전 발행은 `pfm391` 전체 관리자만 수행한다. 공사·설계·구매부서는 원본 근거를 첨부한 수정 요청만 제출한다. 프로젝트별 예외는 기준 사전과 분리하고, 사전 변경은 새 전처리부터 적용하며 변경 전후·요청자·승인자·일시를 감사 로그에 남긴다.
9. **확정 — MVP 승인·보관 형식**: MVP의 공식 결과물은 상세 Excel, 요약 PDF, 원본 근거 Manifest·파일 링크, 원본 파일 보존 묶음으로 한다. 회사 실무 Excel 양식·전자결재·근거 추적 전용 PDF 양식은 후속 연동 범위로 분리한다.
10. **확정 — 상태 용어**: MVP는 `검토 대기`, `승인`, `수정 요청`, `반려`, `추가자료 요청`, `적용 후보`, `적용 보류`, `확정`을 공식 업무 용어로 사용한다. 별도 내부 용어 매핑은 후속 전자결재 연동 시 검토한다.

---

## 18. 참고자료와 결정 기록

- `project_plan.md`: 제품 목적, 단계별 로드맵, 검토 규칙, 보안·운영, 데이터 모델 방향
- `프로젝트_수행흐름_공사비적정성검토.md`: 전처리→후속 검토 흐름, 부서 승인, 도면·단가 처리, API/DB 원칙
- `전처리결과_20260908/119_최종_전처리_종합보고서.md`: 광양5 사무동 전처리 결과·대기 현황·근거 부족 상태
- `전처리결과_20260908/118_final_preprocessing_status.csv`: 도면/수량 234건, 단가 API 86건의 승인·재조회 대기 상태
- `전처리결과_20260908/107_decision_only_queue.csv`: 사람의 판단이 필요한 320행의 승인·수정·추가 근거 요청 대기열

본 PRD의 모든 자동화 기능은 근거, 신뢰도, 규칙 버전, 승인 상태를 함께 보존하는 것을 전제로 한다. 확정되지 않은 결과는 검토를 빠르게 하기 위한 후보이지, 공사비의 공식 확정값이 아니다.
