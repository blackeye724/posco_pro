# 규칙–현재 코드 대조표

기준 문서: [RULE_CATALOG.md](./RULE_CATALOG.md)

## 적용 중인 규칙

| 규칙 | 현재 위치 | 상태 | 확인 내용 |
|---|---|---|---|
| SCOPE-001 | `preprocessing_reader.py::raw_quantity_analysis` | 적용 | 사무동 Building 및 사무동 파일명만 허용 |
| ROW-001 | `raw_quantity_analysis` | 적용 | 내역서 estimate 행을 결과 기준으로 생성 |
| ROW-002 | `raw_preprocessor.py::_is_section_heading` | 적용 | 번호+공사 제목 행 제외, 테스트 있음 |
| SET-001 | `raw_quantity_analysis` | 적용 | 기준/변경 version별 후보 사전 분리 |
| NAME-001 | `quantity_rule_engine.canonical_name` + 사무동 매핑 카탈로그 | 적용 | 신규 업로드와 기존 Reader가 동일한 감사 별칭·표준 키를 문맥 조건과 함께 사용 |
| SPEC-001 | `quantity_rule_engine.canonical_spec` + `spec_compatible` | 적용 | 사무동 승인 매핑의 규격 별칭(AL↔알미늄·바닥/벽 표기)을 품명·단위 문맥과 함께 공통 엔진에서 적용; 미등록 동의어는 후보로 보존 |
| UNIT-001 | `_canonical_unit` | 적용 | 면적·체적·톤·개수 표기 통일 |
| CONTEXT-001 | `narrow_by_context` | 적용 | 코드·동·층·부위·도면 순으로 축소 |
| WORK-001 | `infer_work_package` | 적용 | 사무동 내역 품명·규격의 주요 공종을 분류하고, 명확한 토큰이 없는 행은 미분류로 보존 |
| DRAW-001 | `raw_preprocessor.py::_match_baseline_drawing` | 적용 | 도면번호를 기본키로 하되 건물·공종 패키지가 명시되면 같은 범위만 연결하고, 불명확하면 후보로 보존 |
| DRAW-002 | `/drawings/changes` + 도면 검토 화면 | 부분 적용 | 실제 도면 파일 쌍의 변경 내역 연결 건수·품명을 표시하며 CAD 객체 차이는 자동 확정하지 않음 |
| PRICE-001 | raw 전처리 신규내역 생성 + `/prices/results` | 적용 | 변경 후 신규 사무동 내역만 표준 품명·규격·단위로 기준자료와 재대조해 단가 후보로 만들고 실제 공종·수량·원본행을 표시 |
| PRICE-002 | `/prices/results` 참고단가 매칭 | 적용 | 동일 품명·규격·단위 참고자료를 우선순위로 평가해 연결 건수·최우선 출처·후보 단가를 표시하되 원본 후보 단가에는 복사하지 않음 |
| GROUP-001 | `quantities_by_key_version` 및 표준화 그룹 합계 | 적용 | 같은 표준 그룹의 여러 산출행을 합산 |
| QTY-001 | `quantity_rule_engine.py::compare_quantity` | 적용 | 기준/변경 공통 0.01% 기술 허용오차 |
| QTY-002 | `quantity_rule_engine.py::warning_threshold` → `compare_quantity` | 적용 | 내역 금액구간별 3/2/1% 근사 일치 |
| QTY-003 | quantity API + `/quantities` | 적용 | 내역값·산출값·차이·근거 표시 |
| QTY-004 | `quantity_rule_engine.py::compare_version_quantities` + 기준·변경 대조 패널 | 적용 | 철골 합계를 5톤 미만 2%, 5~50톤 1%, 50톤 초과 0.5%로 판정하고 초과분은 검토 후보로 보존 |
| CAND-001 | `raw_quantity_analysis` | 적용 | 서로 다른 규격·문맥 후보는 합산하지 않고 후보 표시 |
| TRACE-001 | `source_locator`, candidate payload | 적용 | 파일·시트·원본 행 연결 |
| FORM-001 | `_simple_formula_value`, `raw_quantity_analysis` 산식 요약 | 적용 | 모든 수량산출서의 숫자 사칙연산을 재계산하고 일치·차이·미추출을 분리 |
| FORM-002 | `RawPreprocessor.process`, `/upload` | 적용 | 산식·수량 누락 경고 및 추가자료 요청 링크 |
| STEEL-002 | `_steel_coating_formula_summary` | 적용 | 도장·내화 면적 산식 별도 검산 |
| APPROVAL-001 | 승인 API 및 `/approvals` | 기존 구현 보존 | 승인 전 확정 금지 및 단계 잠금 |

## 놓치거나 구조적으로 부족한 규칙

| 규칙 | 문제 | 영향 | 다음 조치 |
|---|---|---|---|
| SPEC-001 | 미등록 현장 약어·재질 동의어는 자동 연결하지 않음 | 승인 근거 없는 추정 매칭을 막기 위해 일부 후보가 남을 수 있음 | 새 동의어는 원본 근거와 적용 범위를 승인받은 뒤 매핑 CSV에 추가 |
| GROUP-001 | `raw_quantity_analysis`의 표준 그룹 키에 공종 관계를 포함 | 같은 품명이 여러 공종에 반복돼도 자료 세트·공종별로 분리해 합산 | 적용 |
| STEEL-001 | `quantity_rule_engine.py::compare_related_totals` + `_steel_relation_summary` | 적용(보류 근거 포함) | 자재·시공 TON 열이 모두 있을 때만 총량 비교하고, 누락 원본도 유효 그룹과 함께 파일별 보류 행으로 표시 |
| 재료–시공 관계 | `classify_material_relation`과 `material_construction_relation_summary` 적용 | 내역서=재료 기준, 수량산출서=시공 근거로 우선 묶고 명시 역할이 있으면 우선 적용; 동일 공종·단위 총량만 후보 비교 | 후보별 근거 행·차이·실제 단위 표시, 철골 TON은 전용 규칙으로 분리 |
| 기준↔변경 대조 | `compare_version_quantities` + `baseline_changed_comparison` | 공종·표준키가 양쪽에 존재하는 내역만 증가·감소·허용오차로 대조 | 화면 표와 원본 행 근거 표시 |
| FORM-001 | 외부 셀 참조·함수·CAD 기반 산식은 자동 계산하지 않음 | 일부 행은 원본 근거 확인이 필요 | 지원 함수 범위를 늘리기 전에 원본·참조 셀 추적 규칙을 추가 |

## 현재 검증 결과

- 코드 parser 버전: raw-v10(번호형 공종 제목 제외·도면 범위/공종 교차 연결 방지 규칙 포함)
- 규격 별칭 엔진: `canonical_spec`가 감사된 사무동 매핑의 품명·단위 문맥을 확인한 뒤 AL↔알미늄 등만 공통 적용
- 기준자료: 583건, 변경자료: 1,840건
- 기준자료 판정: 일치 117건, 불일치 220건, 연결 근거 없음 243건, 재검산 불가 3건
- 변경자료 판정: 일치 356건, 불일치 709건, 연결 근거 없음 775건
- 산식·수량 누락: 13건을 원본 근거와 함께 분리
- 산식 검산: 모든 산식 보유 수량산출서 행을 파일별로 재계산해 일치·차이·원본 수량 미추출을 분리
- 철골 도장·내화: 단순 산식 재계산 결과를 별도 표시
- 철골 TON 총량: 현재 원본 열 부재는 파일별 `원본 분류 열 미추출` 보류 행으로 표시(자동 0 대입 금지)
- 철골 누락 선검증: 유효한 자재·시공 그룹이 있어도 열 누락 파일을 함께 별도 보류 행으로 유지
- 재료–시공 관계 후보: 내역서(재료 기준)·수량산출서(시공 근거)를 공종·단위별로 별도 요약하고, 역할 누락 시 실제 단위와 연결 근거 없음으로 표시
- 기준↔변경 대조: 공종·표준키별 양쪽 내역 수량을 합산해 증가·감소·허용오차 결과와 원본 행을 표시하며, 자료 세트·결과 필터로 405개 대조 행(허용오차 일치 43개)을 조회
- 대조 상세 근거: 선택한 행에서 기준 원본행·변경 원본행·각 수량·증감률을 분리 표시
- 운영 빌드: `/approvals`의 검색 파라미터 Suspense 경계를 보완해 `next build` 성공
- 공종별 필터 검증: 기준 방수 31건, 변경 타일 54건, 변경 철골 105건을 사무동 범위로 확인
- 공종 분류 보강: 고장력볼트·레미콘·압연강판·창호 하드웨어·비계·단열재·엘리베이터 등 명확한 품명 토큰을 분류; 최신 분석에서 미분류 169건은 억지 분류하지 않고 원천 확인 대상으로 유지
- 도면 연계 검증: 실제 파일 쌍을 건물·공종 패키지까지 확인해 연결하고, 도면번호가 일치하는 사무동 변경 내역 행 수·품명을 연결 근거로 반환(자동 승인·수량 확정 없음)
- 신규내역 단가 연결: 변경자료 신규 후보를 `변경자료 신규내역`으로 통일하고 공종별로 분류; 현재 API 응답에서 616건의 전처리 신규 후보가 확인되며 수량·단가 확정은 하지 않음
- 자료 범위 표시: 별도 프로젝트를 만들지 않고 수량·도면·검토 큐·단가 화면에서 `전체/운영 자료/통합 테스트` 필터로만 구분
- 자료 범위 유지: 선택한 범위를 `dataScope` URL 파라미터에 보존해 화면 이동·새로고침 후에도 같은 필터를 복원
- 그룹 키 보강: 같은 표준 품명이 여러 공종에 반복될 때 자료 세트·공종별로 수량산출서 후보를 분리해 잘못된 교차 합산을 방지

## 3단계 진행 상태

- 수량 판정(정확 일치·허용오차 내 근사·불일치·수량 누락), 표준 키·단위·규격
  비교, 단순 산식 재검산은 `backend/app/services/quantity_rule_engine.py`의
  순수 함수로 추출했다.
- `PreprocessingReader`는 해당 엔진을 호출하므로 기준자료·변경자료가 같은
  판정 결과와 차이율을 사용한다.
- 과거 사무동 매핑 파일을 조회해 표준명을 선택하는 정책과 공사관계 그룹화는
  Reader에 남아 있다. 도면 쌍은 공통 전처리에서 건물·공종 범위를 먼저 좁힌 뒤
  번호를 비교하도록 고정했으며, CAD 객체 차이는 원본 근거 확인 전까지 후보로 둔다.

이 표에서 `놓치거나 구조적으로 부족한 규칙`을 다음 단계 공통 엔진의 최소 범위로 삼는다.
