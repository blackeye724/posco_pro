# PRD 비판적 검토

대상 문서: `PRD.md` (2026-09-09 기준)

현재 PRD는 제품 방향, 원본 추적성, 승인 전 자동 확정 방지, 부서 승인 순서가 비교적 명확하다. 다만 구현팀과 실제 검토자가 같은 동작을 기대하려면 다음 항목을 더 구체화해야 한다.

| 번호 | 문제점 | 왜 문제인가 | 수정안 |
|---:|---|---|---|
| 1 | 사용자 문제가 “자료가 분산되어 있다” 수준에 머문다. | 어떤 사용자가 어떤 시간을 줄이고 무엇을 결정하는지가 불명확해 우선순위를 정할 수 없다. | 공사·설계·구매별 현재 작업, 입력, 판단, 완료 결과와 MVP 목표(처리시간·추적성·승인 누락률)를 명시한다. |
| 2 | “광양5 전체”와 “다른 프로젝트도 지원”이 함께 있으나 기본 조회 범위가 불명확하다. | 다른 프로젝트 단가나 도면이 실수로 섞이면 검토 결과가 오염될 수 있다. | 모든 요청에 `project_id`를 필수화하고, 타 프로젝트 자료는 명시적 참고 조회만 허용한다. 화면도 현재 프로젝트를 고정 표시한다. |
| 3 | 역할 표에 ‘전체 관리자·승인권자’, ‘관리자’, ‘CM/검토자(선택)’가 함께 있다. | MVP 계정은 4개인데 역할이 6개라 권한 구현과 테스트 대상이 충돌한다. | MVP 역할을 공사·설계·구매·마스터 4개로 고정하고, CM/조회 사용자는 후속 역할로 분리한다. |
| 4 | `APPROVED`, `확정`, 부서 승인, 전체 최종 확정의 관계가 모호하다. | 승인 API가 단가·수량을 확정해 버리거나, 반대로 승인 후에도 영원히 확정되지 않을 수 있다. | `부서 승인 상태`와 `값 확정 상태`를 분리한다. 최종 확정은 모든 게이트와 필수 근거를 만족한 뒤 마스터가 별도 실행하며, 자동 확정은 금지한다. |
| 5 | 반려·수정 요청의 되돌림 대상과 부분 승인 규칙이 서술식이다. | 여러 경고가 한 검토 묶음에 있을 때 일부만 승인 가능한지 구현자가 다르게 해석한다. | 검토 단위를 `finding` 또는 명시적 `review_item`으로 고정하고, 상태 전이표·부분 성공 응답·잠금 규칙을 정의한다. |
| 6 | 일괄 승인과 개별 승인의 원자성·동시성 규칙이 부족하다. | 두 사용자가 동시에 처리하면 한 항목이 중복 승인되거나 일부만 반영될 수 있다. | `idempotency_key`, 행 잠금/버전 비교, 항목별 결과를 의무화하고 일괄 처리는 전체 원자성이 아닌 항목별 부분 성공으로 고정한다. |
| 7 | 수량·금액 임계값의 “% 또는 금액” 의미가 모호하다. | `5% 또는 30만원`이 OR인지 AND인지, 반올림·부가세·음수에 어떻게 적용하는지 테스트할 수 없다. | 금액 밴드, 계산식, 통화/부가세 기준, 반올림 규칙, 일반/높음 판정식을 수식으로 고정하고 관리자 버전을 붙인다. |
| 8 | `신뢰도`의 의미·범위·표시 기준이 정의되지 않았다. | 확률로 오해하거나 자동 확정 근거로 사용할 위험이 있다. | 0~1의 후보 근거 점수로 정의하고 구성 요소(문자·위치·버전·단위 일치)를 저장한다. 확정 판단에는 사용하지 않는다. |
| 9 | 도면 변경 자동화의 IoU 0.90 목표에 라벨·평가셋·측정 시점이 없다. | MVP에서 달성 여부를 판정할 수 없고, 완전 자동 DWG 분석 제외와도 충돌한다. | MVP는 후보 생성·근거 제공만 합격으로 하고, 정확도 평가는 별도 라벨셋이 생긴 뒤 단계 2에서 시행한다. |
| 10 | PDF·DWG·엑셀 전처리의 입력 계약이 부족하다. | 어떤 파일을 묶어야 하는지, OCR 실패·암호 파일·매크로·외부 링크를 어떻게 처리할지 알 수 없다. | 업로드 묶음의 필수 메타데이터, 지원/비지원 파일, 파일별 상태, 부분 성공, 재시도/취소를 정의한다. |
| 11 | ZWCAD/CAD 뷰어 재사용은 선언되어 있지만 워커 계약이 없다. | Windows 라이선스, 실행 옵션, 프로세스 고착, 결과 포맷이 운영 장애가 된다. | CAD 워커를 별도 프로세스로 정의하고 입력/출력 manifest, 도구 버전, 제한시간, 라이선스 오류, 메타데이터 fallback을 명시한다. |
| 12 | 전처리 결과 저장과 재검토 회차의 관계가 부족하다. | 동일 파일 재처리 결과가 과거 승인 결과를 덮어쓸 수 있다. | `source_version`과 불변 `review_run`을 분리하고, 재처리는 새 회차만 생성하며 과거 회차는 읽기 전용으로 보존한다. |
| 13 | 단가 우선순위는 있으나 “동일 프로젝트 타건물”의 유효성 조건이 없다. | 시기가 다른 계약단가가 자동 후보가 되어 잘못된 가격 인상을 줄 수 있다. | 품명·규격·단위·공종·기준일·계약조건 일치 요건과 시기 차이 경고를 명시하고, 후보와 적용 판단을 분리한다. |
| 14 | 공식 CSV/표준시장단가의 출처·갱신·스키마 오류 처리가 없다. | 오래되거나 잘못 매핑된 파일을 공식 자료로 오인할 수 있다. | 발행기관·기준일·파일 해시·스키마 버전·적재자·유효기간을 저장하고, 스키마 오류는 단가 후보를 생성하지 않게 한다. |
| 15 | 조달청 API 오류는 있으나 키 미설정·응답 스키마 변경·부분 결과 규칙이 없다. | 운영에서 “실패”와 “검색 결과 없음”을 구분하지 못한다. | `CONFIG_ERROR`, `AUTH_ERROR`, `FORBIDDEN`, `NO_RESULT`, `SCHEMA_ERROR`, `TIMEOUT`, `RATE_LIMITED`, `UPSTREAM_ERROR`를 분리한다. |
| 16 | 챗봇이 MVP 핵심 목록에 포함되지만 모델, 비용, 데이터 반출, 답변 품질의 출시 조건이 부족하다. | 챗봇이 검토 엔진보다 먼저 출시되거나 근거 없는 답을 만들 위험이 있다. | 읽기 전용·근거 인용·범위 고정·외부 모델 차단 기본값을 정하고, 근거 없음/권한 부족/모델 장애 테스트를 출시 게이트로 둔다. |
| 17 | 예외 상황이 부족하다. | 스토리지 장애, 바이러스 검사 불가, 작업 취소, 세션 만료, 규칙 버전 불일치, DB 재시작 등에서 사용자 상태가 불명확하다. | 오류 코드·재시도 가능 여부·사용자 조치·멱등성·보상 처리 표를 추가한다. |
| 18 | 성능 수치가 “목표 2초/10초”뿐이고 측정 조건이 없다. | 파일 크기·동시 사용자·페이지 수에 따라 결과가 크게 달라 합격 여부를 판단할 수 없다. | 목록 API p95, 업로드 접수, 작업 대기시간, 동시 사용자, 데이터량을 시험 조건으로 고정한다. |
| 19 | 보존 7년·백업 정책이 있으나 복구 목표와 삭제 보류가 없다. | 실제 장애 시 어느 정도 손실을 허용하는지, 법적 보류 중 삭제를 막는지 알 수 없다. | RPO/RTO, 복구 테스트 주기, 법적 보류 플래그, 물리 삭제 2단계 승인을 정의한다. |
| 20 | 테스트가 기능 목록 중심이며 각 입력·출력·경계값이 없다. | “동작한다”는 말은 남지만 자동화된 합격/불합격을 만들기 어렵다. | 각 핵심 기능에 Given/When/Then 인수 기준과 최소 fixture를 붙이고, 실제 코드 구현 여부를 별도 추적한다. |
| 21 | 원본 링크의 보안·만료·다운로드 범위가 모호하다. | 경로가 노출되거나 승인되지 않은 원본을 내려받을 수 있다. | 파일 경로를 직접 노출하지 않고 단기 서명 URL/권한 확인을 사용하며 다운로드도 감사한다. |
| 22 | 자동 학습 제외와 챗봇 외부 모델 사용이 동시에 존재한다. | 외부 모델 전송이 학습으로 오해되거나 미승인 자료가 전송될 수 있다. | 외부 모델 사용 기본값을 OFF로 하고, 전송 필드 allowlist·보존기간·공급자 정책 동의를 명시한다. |
| 23 | 공식 결과물(PDF/Excel/Manifest)과 화면·DB의 기준 시점은 정의됐지만 생성 실패/재생성 규칙이 없다. | 보고서가 서로 다른 시점의 데이터를 포함할 수 있다. | `export_id` 스냅샷을 트랜잭션으로 고정하고 실패 시 부분 파일을 폐기하며 재생성은 새 ID로만 허용한다. |
| 24 | “예쁘게/편리하게”에 해당하는 사용성 요구의 측정 기준이 부족하다. | UI 품질이 개인 취향에 따라 판단된다. | 핵심 업무 완료 클릭 수, 키보드 접근성, 오류 메시지 노출, 모바일 범위, 한국어 용어 사전을 측정한다. |

## 우선순위

출시 차단 항목은 2, 4, 6, 7, 10, 11, 12, 13, 15, 16, 17, 20이다. 1차 MVP에는 핵심 검토·승인·추적성·재현성만 넣고, 완전 DWG 의미 해석, AI 학습, 회사 전자결재 연동은 후속으로 둔다.

