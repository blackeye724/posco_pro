# MVP 최종 QA 체크리스트

검증 기준일: 2026-09-12  
검증 범위: `광양5 사무동` · 현재 등록된 운영 자료 + 데모 통합 테스트 자료  
원칙: 수량·금액·단가는 승인 전 후보값이며, 타건물 자료는 단가 참고용으로만 사용한다.

## 1. 핵심 업무 흐름

| 단계 | 화면 | 확인 기준 | 결과 |
| --- | --- | --- | --- |
| 01 최초 자료 | `/quantities?sourceSet=기준자료` | 내역서 행을 기준으로 품명·규격·단위·수량을 수량산출서 근거와 대조 | 통과 |
| 02 설계변경 | `/drawings` | 기준·변경 도면의 번호·시트·Rev.와 변경 후보를 표시하고 CAD 자동 확정은 하지 않음 | 통과 |
| 03 신규내역 | `/prices?sourceSet=변경자료` | 변경 후 신규내역의 변경수량·차이·참고단가·출처를 후보값으로 표시 | 통과 |
| 통합 큐 | `/review` | 내역·도면·단가 후보를 단계별로 나누고 자료 범위별 건수를 일치시킴 | 통과 |

## 2. 데모자료 대표 검증

- `통합 테스트` 범위: 내역 11건(기준 4건·변경 7건), 기준·변경 대조 7개 그룹, 신규내역 단가 7건
- 데모 철골보: 기준 12.5톤·변경 12.6톤, 0.80% 차이로 철골 허용오차 내 판정
- 데모 방수: 기준 480·변경 520, 차이 40과 증감률 8.33% 표시
- 데모 내역서 행: 내역 수량·산출 수량·차이·원본 파일/시트/행 근거를 함께 표시
- 데모 도면: 실제 CAD 형상 파일이 없으므로 후보 0건으로 표시하고 자동 판정하지 않음

## 3. 범위 분리

- `전체 / 운영 자료 / 통합 테스트` 필터를 대시보드·수량·도면·통합 큐·단가·승인 화면에 적용
- 화면 이동 시 `dataScope` URL 파라미터를 유지
- 통합 테스트 화면에는 운영 자료의 도면 후보가 섞이지 않음
- 승인 화면에서 `data_origin`이 없는 기존 경고는 파일명·상세·규칙 ID를 이용해 통합 테스트 원본을 보조 식별

## 4. 자동 검증 명령

```powershell
# 백엔드
cd backend
.\.venv\Scripts\python.exe -m pytest -q

# 프론트엔드
cd frontend
npm run build
```

현재 결과: 백엔드 `40 passed`, 프론트엔드 빌드 성공, DB/API health 정상.

## 5. 최종 화면 흐름 확인

- 기본 진입(`/`)은 `01 최초 자료`를 선택한 상태로 시작하고, 기준자료 수량 검토 링크(`/quantities?sourceSet=기준자료`)를 표시한다.
- 대시보드에서 `02 설계변경`을 선택하면 변경 검토 대상 1,851건과 설계변경 큐(`/review?stage=change`)를 표시한다.
- 대시보드에서 `03 신규내역`을 선택하면 신규내역 단가 후보 624건과 단가 검토 링크(`/prices?sourceSet=변경자료`)를 표시한다.
- 확인 요청함의 단계 탭과 승인 대기열의 단계 탭은 동일한 실제 데이터 건수를 사용하며, 선택 단계·자료 범위는 URL 파라미터로 유지된다.

## 6. 제한사항 및 다음 자료 투입 시 확인

- 실제 신규자료 업로드는 이번 검증 범위에 포함하지 않았다.
- 실제 DWG 객체 비교는 CAD 워커가 없으므로 메타데이터·원본 경로·후보 상태까지만 처리한다.
- 신규자료가 들어오면 업로드 → 전처리 실행 → 사무동 범위 확인 → 내역서 행 기준 매칭 → 수량산출서 근거/차이 → 도면 후보 → 신규내역 단가 순서로 재검증한다.
- 허용오차 밖의 철골 총량, 재료·시공 관계, 외부참조·복합산식은 자동 확정하지 않고 근거 확인 대상으로 남긴다.
