# Stitch용 화면 생성 프롬프트 안

목적: 투자사업 공사비 적정성 검토 MVP의 Next.js 화면 설계와 시작 코드를 생성한다.

## 공통 전제

- Stitch가 만드는 것은 완성된 운영 서비스가 아니라 화면 설계와 시작 UI 코드다.
- API, PostgreSQL, 파일 업로드, 인증, 승인 저장은 기존 FastAPI/Next.js 코드에서 연결한다.
- 모든 화면은 한국어이며 PC 우선, 1440px 기준으로 설계하고 1280px에서도 가로 스크롤 없이 동작한다.
- 서비스명은 `투자사업 공사비 적정성 검토`다. 설비 이상탐지 서비스로 표현하지 않는다.
- 사용자는 공사부서 `pfc391`, 설계부서 `pfe391`, 구매부서 `pfp391`, 마스터 `pfm391` 네 계정이다.
- 기본 프로젝트는 `광양5 사무동`이며, 프로젝트·건물·공사/공종·내역 범위를 화면 상단에서 고정 표시한다.
- 원본 파일·시트·행·셀 또는 도면번호·시트·Rev.·페이지·좌표·파일 해시를 근거 패널에 표시한다.
- 승인 전 수량·금액·단가는 확정값처럼 보이면 안 된다. `검토 후보`, `승인 대기`, `확정 전`, `적용 보류` 배지를 사용한다.
- 초록색 확정 표현은 최종 승인 후에만 사용한다. 경고는 색상만으로 구분하지 말고 아이콘과 텍스트를 함께 사용한다.
- 표와 상세 근거 패널을 우선하고, 장식적인 그래프·큰 숫자 카드·가짜 AI 자동판정 문구는 최소화한다.
- 샘플 데이터는 실제 원본 파일을 복사하지 말고 다음처럼 비식별화한다: `기계실 배관 공사`, `A-101`, `Rev.03`, `수량산출서_예시.xlsx`, `42행`.

---

## 안 1: 전체 서비스 한 번에 생성

```text
투자사업 공사비 적정성 검토 MVP의 PC용 내부 웹 화면을 만들어줘.

한 번에 다음 화면을 포함해줘.
1) 네 계정 로그인 화면
2) 프로젝트 현황 대시보드
3) 자료 업로드·버전 관리
4) 도면 변경부위 검토
5) 수량·산식·금액 검토
6) 신규내역 단가 검토
7) 공사부서→설계부서→구매부서 승인 대기열
8) 검토 이력·감사 로그
9) 근거 기반 검토 챗봇

좌측에는 고정 사이드바를 두고 대시보드, 자료, 도면 변경, 수량·금액, 신규내역 단가, 승인 대기열, 검토 이력, 챗봇을 배치해줘. 상단에는 현재 사용자, 부서, 프로젝트, 건물 필터, 자료 회차, 알림을 보여줘.

대시보드는 전체 경고 수, 검토 대기, 부서별 승인 대기, 전처리 작업 상태를 보여주되, 승인 전 수량·금액·단가를 확정 합계처럼 보여주지 마. 메인 영역은 통합 검토 큐 테이블로 만들고 심각도, 대상, 상태, 담당 부서, 최신 근거 시점을 보여줘.

각 검토 화면에는 목록과 우측 상세 근거 패널을 배치해줘. 상세 패널에는 경고 사유, 원본 파일명, 파일 해시, 시트·행·셀 또는 도면번호·시트·Rev.·페이지·좌표, 원본값, 표준화값, 규칙 버전, 신뢰도, 승인 이력을 보여줘.

도면 화면은 기준/변경 도면을 나란히 비교하고 도면 변경 후보, GRID/ZONE, 층, 공간, 연결된 수량산출서 행과 내역서 항목을 보여줘. 연결이 없으면 ‘불가’가 아니라 ‘연결 후보’ 또는 ‘추가 확인 필요’로 표시해줘.

수량 화면은 산식 원문, 재계산값, 보고값, 차이율, 금액 차이, 단위·규격 불일치, 원본 행을 표로 보여줘.

단가 화면은 동일 프로젝트 다른 건물 승인 참고단가, 공식 조달청 CSV·표준시장단가, 조달청 API 결과 순서로 후보를 보여주고 출처·기준일·오류 상태를 표시해줘. 자동 적용 버튼은 만들지 말고 ‘적용 후보’, ‘적용 보류’, ‘구매부서 검토’만 제공해줘.

승인 대기열은 개별 승인과 선택 일괄 승인을 지원하되, 현재 단계가 아닌 승인 버튼은 비활성화해줘. 일괄 처리 결과는 성공/실패 항목별로 보여줘.

챗봇은 현재 프로젝트·건물·공사 범위로 고정하고 답변마다 근거 링크를 보여줘. 승인·반려·단가 확정·원본 수정 요청에는 실행하지 말고 관련 검토 화면으로 이동시켜줘.

전체 디자인은 차분한 공공·기업용 네이비/회색 기반, 높은 가독성, 충분한 여백, 표 중심으로 만들어줘. 실제 API 연결 대신 명확한 mock 데이터 구조와 loading/empty/error 상태를 포함해줘.
```

### 안 1의 장점과 한계

- 장점: 전체 서비스 구조를 한 번에 볼 수 있다.
- 한계: 화면 수가 많아 Stitch가 각 화면을 얕고 비슷하게 만들 수 있다. 첫 결과에서 상세 근거·승인 상태가 부족할 가능성이 있다.

---

## 안 2: 통합 검토 워크스페이스 중심 생성(추천)

```text
투자사업 공사비 적정성 검토 MVP의 핵심 검토 워크스페이스를 만들어줘.

이 결과는 완성된 서비스가 아니라 Next.js에 연결할 수 있는 화면 설계와 시작 코드다. 가장 중요한 화면은 ‘프로젝트 전체 통합 검토 큐 + 우측 원본 근거 패널’이다.

PC용 1440px 레이아웃으로 다음 구조를 만들어줘.

왼쪽 240px: 서비스 로고와 메뉴
- 프로젝트 현황
- 통합 검토 큐
- 도면 변경
- 수량·산식·금액
- 신규내역 단가
- 승인 대기열
- 검토 이력
- 검토 챗봇

상단 64px: 현재 사용자(예: pfc391 / 공사부서), 프로젝트(광양5 사무동), 건물(전체/선택), 공사·공종, 자료 회차, Rev. 필터, 알림.

중앙 메인 영역:
1. 상단에 ‘검토 대기’, ‘높음 경고’, ‘추가자료 요청’, ‘공사/설계/구매 승인 대기’ 요약을 작은 카드로 배치한다. 금액을 큰 확정 합계처럼 강조하지 않는다.
2. 아래에 통합 검토 큐 테이블을 배치한다. 컬럼은 심각도, 검토 대상, 공종, 건물/층, 상태, 현재 승인 단계, 원본 위치, 신뢰도, 최근 변경 시각이다.
3. 행을 선택하면 우측 420px 상세 패널이 열린다.

우측 상세 근거 패널:
- 제목: ‘왜 검토가 필요한가’
- 경고 사유와 적용 규칙 ID/버전
- 상태 배지: 검토 후보, 승인 대기, 추가 확인 필요, 적용 보류, 확정 전
- 원본 파일명·SHA-256·자료 유형·기준일
- 엑셀: 시트명·행·셀 / 도면: 도면번호·시트·Rev.·페이지·좌표
- 원본값과 표준화값 비교
- 연결 후보: 도면 변경 → 수량산출서 → 내역서 → 단가
- 신뢰도는 ‘근거 강도’라고 표기하고 확정 여부로 표현하지 않는다.
- 하단에 ‘원본 열기’, ‘검토 화면으로 이동’, ‘추가자료 요청’ 버튼을 둔다.

선택 행의 유형별 상세 콘텐츠도 만들어줘.
- 도면 변경: 기준/변경 썸네일, 도면번호, 시트, Rev., GRID/ZONE, 변경 후보 설명
- 수량: 산식 원문, 재계산값, 보고값, 차이율·금액, 단위·규격 경고
- 단가: 동일 프로젝트 다른 건물 참고단가 → 공식 CSV/표준시장단가 → 조달청 API 순서, 각 출처와 기준일
- 승인: 공사→설계→구매 단계 타임라인과 담당자 의견

다음 상태도 반드시 설계해줘.
- loading: 테이블 skeleton과 작업 진행률
- empty: ‘현재 범위에 검토 대상이 없습니다’와 필터 초기화
- error: 오류 코드·요청 ID·재시도 버튼
- permission denied: 필요한 부서 권한 안내
- stale version: ‘최신 회차를 다시 불러오세요’ 안내

검토 대기열에서 개별 승인과 선택 일괄 승인을 지원하지만, 현재 부서 단계가 아니면 버튼을 비활성화한다. 일괄 처리 후에는 성공/실패 항목별 결과를 표시한다.

한국어 공공·기업용 디자인, 네이비·회색 기반, 흰 배경, 얇은 테두리, 높은 대비, 표 중심으로 만들어줘. 둥근 카드와 장식 그래프는 최소화하고, 화면이 처음 열렸을 때 사용자가 ‘무엇을 검토해야 하는지’와 ‘근거가 어디인지’를 5초 안에 파악할 수 있게 해줘.

사용할 mock 데이터:
- 프로젝트: 광양5 사무동 / 건물 전체 / 기계실 배관 공사
- 도면: A-101 / Rev.03 → Rev.04 / 2층 기계실 / GRID B-4
- 수량산출서: 수량산출서_예시.xlsx / 수량 시트 / 42행
- 상태: 수량 검토 대기, 설계 근거 확인 대기, 단가 적용 보류
- 단가 후보: 동일 프로젝트 타건물 참고, 공식 표준시장단가, 조달청 API 결과 없음

실제 API·DB·로그인·파일 다운로드를 구현했다고 표시하지 말고, 연결 지점을 TODO 또는 mock service로 분리해줘.
```

### 안 2를 추천하는 이유

- 실제 검토 업무의 핵심은 화면 수가 아니라 “검토 대상 → 원본 근거 → 승인 상태”를 한 번에 연결하는 것이다.
- 현재 FastAPI API와 기존 Next.js 화면을 확장하기 쉽다.
- Stitch가 긴 프롬프트에서 기능을 누락할 위험을 줄이면서도 공사·설계·구매 업무를 모두 표현한다.
- 이후 생성된 디자인 시스템을 기준으로 업로드·도면·수량·단가·이력 화면을 개별 생성할 수 있다.

---

## 안 3: 승인 대기열·근거 확인 중심 생성

```text
투자사업 공사비 적정성 검토 MVP의 부서 승인 대기열 화면을 만들어줘.

핵심 사용자는 공사부서, 설계부서, 구매부서와 마스터다. 화면 목적은 승인자가 한 항목씩 또는 선택 항목을 일괄 검토하되, 승인 전 값을 확정값으로 오해하지 않게 하는 것이다.

레이아웃:
- 상단: 프로젝트·건물·공사·자료 회차·Rev. 필터와 현재 사용자/부서
- 왼쪽 55%: 승인 대기 항목 테이블
- 오른쪽 45%: 선택 항목의 원본 근거와 연결 관계

테이블 컬럼: 선택 체크박스, 심각도, 검토 대상, 금액(‘후보’ 배지 포함), 현재 단계, 요청일, SLA 상태, 근거 상태.

상세 패널에는 다음을 보여줘.
- 도면 변경 전/후 도면번호·시트·Rev.
- 도면번호·페이지·좌표 또는 파일명·시트·행·셀
- 원본값/표준화값/재계산값
- 도면 변경 후보 → 수량산출서 → 내역서 → 단가 후보 연결
- 판정 사유, 신뢰도, 적용 규칙 버전, 이전 승인 이력

승인 버튼은 현재 단계에 맞게 보여줘.
- 공사부서: 승인, 수정 요청, 반려, 추가자료 요청
- 설계부서: 변경근거 확인, 수정 요청, 반려, 추가자료 요청
- 구매부서: 단가 적용 후보, 적용 보류, 수정 요청, 반려
- 마스터: 예외 승인/단계 우회는 사유 입력을 요구

선택 일괄 승인 모달에는 대상 수, 필수 근거 누락 수, 상태 충돌 수를 먼저 보여주고 실행 후 항목별 성공/실패 결과를 보여줘. 승인 순서가 맞지 않으면 승인 버튼을 비활성화하고 이유를 툴팁으로 보여줘.

상태는 검토 대기, 검토 중, 승인, 수정 요청, 반려, 추가자료 요청, 적용 후보, 적용 보류, 확정 전을 사용해줘. 승인 전 금액·수량·단가는 흐린 확정 합계나 초록색 완료값으로 표시하지 말고 ‘후보값’으로 표시해줘.

오류 상태도 설계해줘: 권한 없음, 최신 회차 아님, 원본 근거 누락, 조달청 조회 실패, 일괄 처리 부분 실패. 각 상태에 오류 코드·요청 ID·재시도/새로고침 안내를 포함해줘.

실제 백엔드 연결 대신 mock 데이터와 컴포넌트 구조만 만들고, Next.js API 연결 위치를 주석으로 남겨줘. 한국어 PC용 기업 업무 화면이며 장식보다 정확한 정보 계층과 추적성을 우선해줘.
```

### 안 3의 장점과 한계

- 장점: 승인 순서·근거·일괄 처리 UX를 가장 구체적으로 검증할 수 있다.
- 한계: 전체 서비스 탐색 구조와 업로드/전처리/대시보드 방향을 별도 설계해야 한다.

---

## 추천 결론

최선안은 **안 2**다.

안 2를 Stitch에 먼저 입력해 통합 검토 워크스페이스와 디자인 시스템을 만든다. 그 결과가 확정되면 다음 순서로 화면을 추가 생성한다.

1. 안 2 결과를 기준으로 승인 대기열을 안 3 방식으로 구체화
2. 자료 업로드·버전 관리 화면 추가
3. 도면 변경 검토 화면 추가
4. 수량·산식·금액 검토 화면 추가
5. 신규내역 단가 검토 화면 추가
6. 검토 이력·보고서·챗봇 화면 추가

처음부터 9개 화면을 한 번에 만들면 화면은 많아지지만 실제 근거·상태·권한 표현이 약해질 수 있다. 통합 검토 큐를 먼저 만드는 것이 현재 업무의 중심과 기존 FastAPI 구조를 가장 잘 연결한다.

## Stitch에 같이 제공할 파일

### 필수

1. `PRD_v2.md`: 서비스 목적, 사용자, 상태, 근거 표시, 오류, 인수 기준의 기준 문서
2. 이 문서의 **안 2** 프롬프트
3. 현재 스타일을 유지해야 한다면 `frontend/app/globals.css`, `frontend/app/layout.tsx`, `frontend/components/api.ts`
4. 대시보드·승인 대기열·경고 상세의 화면 캡처 2~3장

### 선택 제공

- `frontend/app/page.tsx`, `frontend/app/approvals/page.tsx`, `frontend/app/drawings/page.tsx`, `frontend/app/quantities/page.tsx`, `frontend/app/prices/page.tsx`, `frontend/app/history/page.tsx`: 기존 라우트와 화면 구조를 유지할 때
- `README.md`: 로컬 실행 주소와 FastAPI 연결 구조를 설명할 때
- 실제 파일 대신 비식별화된 샘플 JSON 1개: 경고·근거·승인 대기열의 필드 모양을 보여줄 때

### 제공하지 말아야 할 파일

- `.env`, 조달청 API 키, DB 비밀번호, JWT secret
- PostgreSQL dump와 운영 DB 접속정보
- 실제 원본 PDF·DWG·엑셀(계약단가·개인정보가 포함된 파일)
- `node_modules`, 빌드 산출물, 대용량 전처리 결과 전체

실제 원본 대신 `A-101`, `Rev.03`, `수량산출서_예시.xlsx`, `42행`, `기계실 배관 공사`, `검토 후보` 정도의 샘플만 사용한다.
