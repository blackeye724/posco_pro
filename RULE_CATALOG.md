# 광양5 사무동 수량 검토 규칙 목록

이 문서는 기존 전처리 산출물과 PRD에서 확인된 규칙의 기준 목록이다. 이 목록에 없는 규칙은 자동 판정에 사용하지 않는다. 모든 결과는 승인 전 검토 후보이며 수량·금액·단가를 확정하지 않는다.

## 적용 범위

- 프로젝트: `project-g5-office` / 광양5 사무동
- 1차 기준 행: 내역서의 각 원본 행
- 보조 근거: 같은 자료 세트의 수량산출서 원본 행
- 기준자료와 변경자료는 각각 독립적으로 비교한다. 기준↔변경 비교는 별도 대조 단계에서만 수행한다.
- 타건물 내역·수량 행은 가격 참고용으로만 보관하며 사무동 수량 연결에 사용하지 않는다.

## 확정 규칙

| ID | 규칙 | 판정 방법 | 원본 근거 |
|---|---|---|---|
| SCOPE-001 | 사무동 범위 제한 | 건물=`사무동`인 내역서·수량산출서만 연결 | PRD 핵심 업무 규칙, 현재 raw 전처리 |
| ROW-001 | 내역서 행 우선 | 결과 한 건은 내역서 원본 행으로 만들고 산출서는 근거로 붙임 | 사용자 확정 요구, PRD §6 |
| ROW-002 | 공종 제목 제외 | `0306. 타일공사`처럼 번호+공사이고 단위·산식·수량이 없으면 검토 행에서 제외 | `raw_preprocessor._is_section_heading`, 테스트 |
| SET-001 | 자료 세트 격리 | 내역서와 수량산출서는 동일 `기준` 또는 동일 `변경` 세트 안에서만 연결 | 사용자 핵심 업무 규칙 |
| NAME-001 | 표준품명 매핑 | 기존 사무동 매핑의 원본 품명을 표준품명으로 변환하되, 기존 수량·승인값은 가져오지 않음 | `14_사무동_현재검토_표준화매핑.csv` |
| SPEC-001 | 규격 표현 정규화 | 괄호·쉼표·순서 차이를 제거하고 의미 토큰이 한쪽에 포함되면 호환 후보로 둠 | 기존 코드 `_spec_compatible`, 전처리 매핑 |
| UNIT-001 | 단위 동등성 | `M2/㎡/M²`, `M3/㎥/M³`, `톤/TON/T` 등 표기를 표준 단위로 통일 | `20_사무동_매핑_필드대조.csv`, PRD |
| CONTEXT-001 | 문맥 우선 축소 | 건물·층·부위·도면·항목코드가 있으면 같은 문맥의 산출 행만 후보로 남김 | 전처리 산출물 및 현재 구현 |
| WORK-001 | 공종 분류 | 내역 품명·규격의 명확한 공종 토큰을 사용해 필터를 만들고, 불명확한 행은 `미분류·원천 확인 필요`로 보존 | 내역서 공종 분류, 현재 전처리 |
| GROUP-001 | 산출행 그룹 합계 | 표준품명·규격·단위·자료 세트·문맥이 같은 산출 행 여러 개는 합산해 내역서 한 행과 비교 | 사용자 요구, 기존 연결 결과 |
| QTY-001 | 기술적 수량 일치 | `abs(차이) ≤ max(0.0005, 내역수량×0.01%)`이면 일치 | `20_사무동_매핑_필드대조.csv` |
| QTY-002 | 금액 구간 허용오차 | 기술적 일치가 아니어도 내역 금액 `<1천만원:3%`, `1천만~1억원:2%`, `≥1억원:1%` 이내면 `허용오차 내 근사 일치`로 검토 표시 | PRD_v2 §6.2 |
| QTY-003 | 차이 수량 표시 | 내역수량, 산출서 그룹합계, 차이를 모두 표시하며 산출값은 확정값이 아님 | 사용자 요구 |
| QTY-004 | 철골 합계 허용오차 | 동일 공사범위·부재·규격·단위로 합산한 TON을 비교한다. 5톤 미만은 ±2%, 5~50톤은 ±1%, 50톤 초과는 ±0.5% 이내를 `일치(허용오차 내)`로 표시한다. 그 초과~각각 5%, 3%, 2% 이내는 `검토 후보`로 남기고, 그 이상 또는 범위·규격·단위 불일치는 `불일치`로 처리한다. | 사용자 확정 방향, AISC 303-22 총중량·범위 일관성 원칙 |
| CAND-001 | 복수 후보 금지 | 동일 품명이라도 규격·단위·문맥 후보가 여러 개면 자동 합산·확정하지 않고 후보로 표시 | PRD 자동 확정 금지 |
| TRACE-001 | 원본 추적 | 파일명·해시·시트·행·셀 또는 도면 식별정보를 결과에 연결 | PRD 원본 추적 규칙 |
| FORM-001 | 단순 산식 재검산 | 외부 참조·함수·셀 주소가 없는 숫자 사칙연산만 독립 계산 | `75_사무동_단순산식_자동검산결과.csv` |
| FORM-002 | 산식 수량 누락 | 산식은 있으나 원본 수량이 없으면 `원본 수량 미추출`·추가 확인으로 저장 | raw-v9 전처리 규칙 |
| STEEL-001 | 철골 TON 총량 | `물량(TON)·자재구분·부재구분`이 실제 원본에 있을 때만 부재군별 자재·시공 총량 비교 | 기존 전처리 보고서 |
| STEEL-002 | 철골 도장·내화 | 도장·내화 산식은 면적 산식으로 독립 검산하며 철골 TON에 강제 합산하지 않음 | `00_전처리_보고서.md`, 철골 원본 산식 |
| APPROVAL-001 | 승인 순서 | 공사부서→설계부서→구매부서 순서를 지키며 승인 전 값은 후보 상태 | PRD 승인 규칙 |

## 기존 산출물에서 확인했지만 아직 자동화하지 않은 규칙

- 철골 자재·시공 연결: 현재 등록된 `04.철골산출서.xlsx`에는 TON·자재구분·부재구분 열이 없어 판정을 보류한다. 해당 열이 있는 변경 XLSM을 현재 사무동 원본으로 다시 등록하면 STEEL-001을 적용한다.
- 재료–시공 관계: 앵커볼트↔설치, 철골 부재↔가공·조립·세우기, 철골 단면↔방청·내화 도장처럼 관계별 키와 산식 근거를 별도 결과로 생성해야 한다. 단순 품명 일치로 대체하지 않는다.
- 기준↔변경 대조: 같은 표준키의 기준·변경 내역을 연결하되, 변경자료에 직접 근거가 없으면 수량 검토 결과와 섞지 않는다.

## 금지 사항

- 기존 승인 매핑의 수량·단가·금액을 신규 자료에 복사하지 않는다.
- 도면 연결이 없다는 이유만으로 공사 수량을 자동 불가 처리하지 않는다.
- 타건물 자료를 사무동 내역·수량 후보로 사용하지 않는다.
- 산출서 그룹 합계나 산식 재계산값을 승인 확정값으로 저장하지 않는다.

## 다음 구현 기준

이 목록을 기준으로 `quantity_rule_engine.py`를 만들고, 규칙별 입력·출력·근거를 테스트한다. 구현 순서는 `SCOPE/SET → NAME/SPEC/UNIT → CONTEXT/GROUP → QTY → 관계 규칙 → TRACE/승인`으로 고정한다.
